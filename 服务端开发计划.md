## 阶段 11：服务端基础架构 ✅

实现内容：

- 新建 FastAPI 服务端项目，使用 Python 3.12
- 使用 SQLAlchemy 2 管理数据模型，SQLite 作为服务端数据库
- 使用 Alembic 管理数据库版本迁移
- 建立 `User → Trip → Record → RecordImage` 服务端数据关系
- Trip 保存行政区划代码和城市名称，不在服务端重复维护完整行政区划数据
- 保留单一共享账号模型，不提供公开注册功能
- 建立统一配置、异常处理和 `/api/v1/health` 健康检查接口
- 提供 Dockerfile 和 Docker Compose 开发运行环境

回归：执行服务端自动化测试，验证 FastAPI 启动、SQLite 初始化、Alembic 迁移、数据关系和健康检查接口。

进度（2026-09-16）：`server/` 已实现上述基础架构；4 个 pytest 测试通过，`docker compose config --quiet` 通过。图片表只建立元数据关系，图片上传与 COS 留待阶段 14。

## 阶段 12：旅行记录服务端 CRUD ✅

实现内容：

- 实现 Trip 创建、列表、详情、编辑和删除接口
- 实现 Record 创建、详情、编辑和删除接口
- 保持与 Android 当前 `Trip → Record` 数据模型及字段语义一致
- 服务端校验 Trip 起止日期和 Record 日期范围
- 支持 `ATTRACTION`、`FOOD` 两种记录类型
- 保留名称、评分、人民币花费、备注等现有字段
- 删除 Trip 时级联清理 Record，删除最后一条 Record 时保持与现有 App 一致的空 Trip 清理规则
- 提供旅行城市数、出行次数和总花费等统计数据

回归：自动化测试覆盖 Trip/Record CRUD、日期边界、非法参数、级联删除、空 Trip 清理和统计结果。

进度（2026-09-16）：已实现 `/api/v1/trips`、`/api/v1/records`、`/api/v1/stats`；阶段 11～12 共 8 个 pytest 测试通过。Trip 可先创建为空，再添加首条记录；删除最后一条记录会清理 Trip。现阶段使用数据库中的单一用户，业务接口认证在阶段 13 补齐。

## 阶段 13：共享账号认证 ✅

实现内容：

- 实现单一共享账号登录，不开放用户注册
- 密码仅保存安全哈希，不保存明文
- 实现 Access Token 和 Refresh Token
- 为 Trip、Record、图片和 Agent 接口增加认证
- 提供当前用户信息接口
- 提供首次创建共享账号的初始化方式
- 密码、Token Secret 等敏感配置通过环境变量管理，不写入仓库

回归：测试正确/错误密码、Token 签发与刷新、未登录访问、非法及过期 Token，并验证业务接口认证生效。

进度（2026-09-16）：通过 `python -m app.bootstrap` 从环境变量初始化唯一共享账号，密码使用带随机盐的 PBKDF2-SHA256 哈希；提供登录、刷新与当前用户接口，旅行/记录/统计接口要求 Access Token。阶段 11～13 共 12 个 pytest 测试通过。图片和 Agent 接口尚未实现，相关认证随未来接口一起补齐。

## 阶段 14：腾讯 COS 原图存储 ⏳

实现内容：

- 接入腾讯云 COS，旅行照片不存入 SQLite 或 VPS 本地磁盘
- COS 保存用户选择的原始图片，不使用当前 1080px / JPEG 80 压缩副本代替原图
- `RecordImage` 独立保存图片元数据和 COS Object Key
- 每条 Record 服务端限制最多 9 张图片
- 使用 UUID 生成对象名称，避免原文件名冲突
- 支持图片上传、查询和删除，并在 Record 删除时清理对应 COS 对象
- 抽象图片存储接口，避免业务层直接依赖 COS 实现
- 为后续 Android 直传 COS 和预签名 URL 保留扩展能力

回归：自动化测试覆盖图片数量限制、元数据、上传/删除流程和 COS 异常；COS 相关测试使用 Mock，正式环境再进行真实上传验证。

## 阶段 15：Android 服务端接入与同步 🧪（文字记录已实现，照片部分暂缓）

实现内容：

- 增加网络权限并接入 FastAPI
- 使用 Retrofit/OkHttp 实现登录和业务 API 请求
- Android Room 从唯一数据源调整为本地缓存，服务端 SQLite 作为权威数据源
- 将现有 Trip、Record CRUD 接入服务端，并保持当前页面交互不变
- 两台 Android 设备登录同一共享账号后可以读取相同旅行数据
- 调整 `PhotoManager`，保留用户选择的原始图片用于上传 COS
- 当前 1080px / JPEG 80 图片仅作为本地预览图，不再作为云端原图
- 使用 Coil 加载服务端图片
- 优先实现简单可靠的同步策略，不在本阶段实现复杂双向冲突合并

回归：两台设备分别执行新增、编辑和删除旅行记录，验证另一台设备刷新后数据一致；验证 1 张和 9 张原图上传、重启后加载及图片删除。

进度（2026-09-17）：Android 使用 Retrofit 接入登录、Token 刷新和旅行/子记录 API；Room 2→3 为 Trip/Record 增加可空的服务端 ID 并保留旧本地数据。无旧旅行时可在个人中心登录，手动刷新服务端文字记录；云端写入先请求服务器再更新缓存，断网不会变成本地独立写入。云端照片被明确拒绝，原图上传和远程图片展示依赖阶段 14，未实现。API 34 模拟器完成登录、拉取、另一会话更新后刷新、从 App 删除并核对服务端；API 24 模拟器完成包含网络请求模拟的完整仪器回归；两台真实设备和真实服务端弱网尚未验收。本阶段保持 🧪，不把 COS 项目标记完成。

## 阶段 16：智能规划 Agent ⏳

实现内容：

- 将现有智能规划页面接入 FastAPI Agent
- 服务端调用公开 LLM API，不在 VPS 本地部署大模型
- LLM API Key 仅保存在服务端环境变量
- 使用 SSE 向 Android 流式返回 Agent 输出
- 第一版 Agent 支持普通旅行规划对话
- 为 Agent 提供查询 Trip 和 Record 的内部工具，使其能够结合已有旅行记录回答问题
- 保留现有欢迎消息和多行输入交互

第一版暂不接入天气、地图、餐厅、景点搜索等额外外部工具，基础 Agent 稳定后再扩展。

回归：验证普通对话、SSE 流式显示、历史旅行数据查询、LLM 超时和接口异常处理。

## 阶段 17：香港服务器部署 ⏳

实现内容：

- 部署至香港 Ubuntu Linux VPS
- 使用 Docker Compose 运行 FastAPI
- 使用 Caddy 提供 HTTPS 和反向代理
- SQLite 数据库通过宿主机目录持久化，不保存在临时容器层
- 配置腾讯 COS、LLM 和认证相关环境变量
- 配置 SSH Key、防火墙和必要的服务安全策略
- 配置 Docker 自动重启和日志轮转
- 定期备份 SQLite，并将备份保存到 VPS 之外的位置
- Android 正式环境切换到 HTTPS 服务端地址

回归：分别使用 Wi-Fi 和移动网络测试登录、CRUD、图片上传/加载和 Agent SSE；观察跨境网络延迟、丢包以及 VPS CPU、内存、磁盘使用情况。

## 阶段 18：服务端化验收 ⏳

- ⏳ 两台设备共享账号数据一致性测试
- ✅ 50 条以上 Trip/Record 数据加载与滚动压力测试（`LargeDatasetTest` 写入 60 条，并在 API 34、API 24 通过）
- ⏳ 单条 Record 9 张原图上传和加载测试
- ⏳ COS 图片删除及异常恢复测试
- ⏳ Android 弱网、断网和服务端不可用场景测试（已自动化覆盖 503 时保留缓存，实际网络环境待补）
- ⏳ Agent 长回答和 SSE 中断测试
- ✅ SQLite 自动备份及恢复测试（`app.backup` CLI 支持完整性校验、应用表校验、临时文件和原子替换；服务端测试覆盖恢复回滚保护）
- ⏳ 香港 VPS 连续运行稳定性测试
- ✅ API 24 最低版本仪器回归（34/34；真实服务端同步待手工验收）
- ✅ README、需求分析、开发计划、开发日志和测试指南同步更新

当前进度（2026-09-17）：阶段 18 已补齐 60 条记录页面滚动、API 24 最低版本仪器回归、服务端不可用时保留缓存和本地 SQLite 备份恢复的自动化测试。阶段 17 已在 Ubuntu 24.04 VPS 完成 Docker、Compose、FastAPI 容器、SQLite 持久化、Alembic 基础部署和 `shared` 账号初始化；本次临时公网 IP 联调已启用 `0.0.0.0:8000`，服务器本机健康检查通过，公网 HTTP 仍需云安全组和真实设备验证。公网 HTTPS、两台真实设备、实际服务端弱网/断网、COS 和 Agent 仍未完成。

验收完成后，在 [开发日志.md](./开发日志.md) 中记录 Android 设备、网络环境、服务端版本、部署环境、日期和结果。
