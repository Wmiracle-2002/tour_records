# Agent 记忆系统方案（讨论稿）

日期：2026-09-26。当前只制定方案，未实现记忆功能。

## 现状与目标

每次 Android 只向 `POST /api/v1/agent/chat` 发送当前一句话；服务端每次新建 `TravelAgentState`，LangGraph 虽有 `messages` 字段，却没有跨请求的会话 ID 或持久化。Android 消息列表也只保存在当前 ViewModel。因此“南京有什么景点？”之后问“美食呢？”无法稳定继承南京，退出页面后聊天内容还会消失。

目标分成两类：

- **短期会话记忆**：同一个对话内理解“那里、第二天、那美食呢”等指代，并在重新打开 App 后展示该对话。不同对话不自动混用上下文。
- **长期偏好记忆**：跨对话记住用户明确指定的旅行偏好，例如“不吃辣、喜欢博物馆、行程别太赶”。已去过的城市、景点、美食继续以现有 Trip/Record 为准，不能从模型记忆推断或复制一份可能过时的清单。

## 推荐的第一版

### 1. 会话与短期上下文

在服务端 SQLite 增加 `chat_conversations` 与 `chat_messages`，都绑定 `user_id`。聊天请求增加可选 `conversation_id` 和用于重试去重的 `client_message_id`；响应返回 `conversation_id`。旧客户端只传 `message` 仍可正常问答，但不获得跨请求上下文。Android 保留当前会话 ID，提供新建对话、查看历史和删除对话。

每轮只读取当前会话最近约 6～8 轮问答，连同**当前用户消息**送入需求分析；后续仍使用本轮独立的 `TravelRequirement`、信息状态和工具结果。不把上一轮的 POI、天气、预算、ToolMessage 或整个 LangGraph State 直接复用，因为这些事实可能过期。历史记录每次从 Trip/Record 读取；天气、地点与费用按本轮需要重新查询或估算。

失败请求不写入“已完成回答”；网络重试带同一个 `client_message_id` 时返回已有结果或明确的处理中状态，避免重复计费与重复消息。会话查询和删除都按 `user_id` 限定。

### 2. 长期偏好

增加独立的 `user_preferences` 表，首版只支持有限的结构化类别，如饮食忌口、景点兴趣、旅行节奏、预算倾向。仅当用户明确说“记住我不吃辣”或在个人中心主动填写时写入；“朋友不吃辣”“这次不吃辣”不应自动变成永久偏好。每条保存来源、修改时间，可查看、更正、删除；同类新偏好覆盖旧值。

需求分析先读取明确偏好，再和本轮要求合并；**本轮明确要求优先**。例如长期“喜欢慢节奏”，本轮“这次一天多看几个景点”按本轮执行。偏好只能影响推荐排序或约束，不能变成地点、开放时间、门票或用户旅行史等外部事实。

### 3. 为什么先不用向量库或完整 Graph checkpoint

当前单人小应用使用 SQLite 即可按会话 ID、用户 ID 和偏好类别直接查到所需数据；没有大量非结构化资料要做语义搜索。现有 Graph 的状态还包含本轮临时 POI、天气及校验结果，整包恢复容易把旧事实当成新事实。首版持久化用户可见消息与明确偏好即可；只有对话变长后确实出现上下文丢失，再增加可审查的会话摘要。

## 开发顺序与验收

1. **数据与 API**：增加迁移、会话/消息表、可选 `conversation_id`、消息重试去重，以及会话列表/消息/删除接口。测试跨用户隔离、重复请求、旧客户端兼容、删除后不可读取。
2. **短期记忆**：为需求分析加入受长度限制的近期对话，并让 Android 读取/显示历史。测试“推荐南京景点 → 美食呢？”继承南京；新会话不能继承；“明天呢？”天气须重新查明天，不能复用今天结果；上下文不足时要求澄清。
3. **长期偏好**：实现显式写入、修改、读取、删除和个人中心查看。测试“记住我不吃辣”跨会话生效、“这次想吃辣”只覆盖本轮、“我朋友不吃辣”不写入。
4. **回归与部署**：检查请求耗时和 LLM 上下文长度；跑历史、推荐、天气、距离、预算、规划及 401/超时回归。迁移前备份 SQLite；真机检查对话切换和重启后恢复。

## 待一起确认

- 长期偏好首版只记录**用户明确要求记住**的内容，还是允许 Agent 从多轮对话中自动提取候选并由用户确认？推荐先做前者。
- 会话删除是否同时删除从该会话明确保存的长期偏好？推荐分开管理：删除聊天只删除聊天，长期偏好在个人中心单独删除。
- Android 首版是否需要完整的对话列表与新建入口？推荐需要，否则用户无法控制哪段短期上下文继续生效。
