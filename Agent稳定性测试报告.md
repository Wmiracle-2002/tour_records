# Agent 稳定性测试报告

日期：2026-09-26；分支：`fix/agent`。

## 本轮结论

| 计划项 | 本轮验证 | 结果 |
| --- | --- | --- |
| P0 五类问答 | 认证 FastAPI 模拟 Android 请求，覆盖历史、景点/美食推荐、距离、实时/明天天气、预算及无数据降级；公网 HTTPS 健康接口 | 模拟测试通过；健康接口 200。真实模型批量请求未执行。 |
| P0 认证 | 单元/API 测试覆盖登录、401、刷新与请求 ID | 本地通过；手机自动刷新尚未在本轮复测。 |
| P1 一日规划 | 用户已在 Android 上测试，可得到标准内容；本地测试覆盖漏掉餐饮后的补齐和预算中文化 | 基本可用；真实耗时未采样。 |
| P1 二日、三日规划 | 新增四组认证 API 用例：分别有/无公历出发日期；假 LLM、假高德返回已核对的景点与餐饮 | 4/4 通过；天数、日期、类别、请求 ID、两类 POI 收集及无额外 ReAct 决策均正确。真实模型与手机尚未验证。 |
| P2 边界与降级 | 全量服务端测试覆盖错城/同名/无坐标/重复 POI、高德错误与非法参数、天气日期/节日、空数据、LLM 错误/超时、客户端断开等 | 395 passed、1 个第三方弃用警告。 |
| Android 界面 | 将本人消息气泡右下角圆角从 6dp 改为 20dp；运行 JVM 单元测试，编译 Debug APK 与测试 APK | 构建通过；无连接设备，视觉与设备测试待真机确认。 |

公网 `https://www.cq-footmark.online/api/v1/health` 返回 200。新 Debug APK 使用 `https://www.cq-footmark.online/`，路径为 `app/build/outputs/apk/debug/app-debug.apk`。服务端没有本轮代码改动，也没有重新部署。

## 尚未通过真实环境验收

1. 真实 LLM + 高德的五类问答，以及二日、三日规划的回答依据、单次总耗时、阶段耗时和 120/150 秒预算。当前四组规划用例使用假客户端，不能代表真实耗时。
2. Android 真机上的消息气泡外观、401 后自动刷新、弱网/断网后的草稿保留与重发；本机 ADB 没有连接设备。
3. 生产环境认证批量请求被自动审批拒绝：方案会使用服务器现有用户身份与密钥调用外部模型和高德，可能读取旅行历史并消耗额度。没有改用其他方式绕过该拒绝。获得明确授权后再按计划采集请求 ID、HTTP 状态与阶段耗时。

本轮只把“本地模拟通过”标为通过，不把真实链路、真机视觉或超时预算记为已验收。
