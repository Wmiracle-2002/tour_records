# 旅行记录 App — 需求分析文档

> 本文档记录已经确认的产品目标，并将目标需求与当前 Demo 实现分开说明。更新日期：2026-09-15。

---

## 1. 项目背景与目标

- 为个人开发一个记录旅行经历的安卓 App
- **Demo 版目标**：免登录记录去过的城市、景点和美食，支持照片，并预留后续认证和智能规划扩展点
- **远期规划**：接入 AI Agent，分析用户画像和历史旅行经历，推荐下次旅行计划

## 2. 已确认需求（初版，来自用户原始想法）

| # | 需求 | 说明 |
|---|------|------|
| R1 | 城市记录 | App 以"城市"为核心组织维度，记录去过的城市；城市全局唯一，去过几次由记录日期聚合体现 |
| R2 | 景点模块 | 每个城市下可记录若干景点，支持上传照片 |
| R3 | 美食模块 | 每个城市下可记录若干美食，支持上传照片（第 2 轮确认） |
| R4 | 备注字段 | 景点/美食记录均包含备注字段，用于记录个人想法（第 2 轮确认） |
| R5 | 记录字段 | **旅行记录**：城市 + 开始日期 + 结束日期；**子记录**：名称 + 类型（景点/美食）+ 日期 + 评分（1-5 星，可选）+ 花费（人民币，可选）+ 备注 + 照片（第 3 轮确认；第 4 轮砍掉位置字段、不加标签；**第 10 轮确认两层嵌套模型**） |
| R6 | 照片规则 | 每条子记录最多 9 张；入库前压缩（约 1080px 长边，JPEG 质量 80），不保留原图；存储在 app internal storage；删除记录时同步删除对应照片文件（第 4 轮确认；**第 10 轮补充细节**） |
| R7 | 用户登录 | Demo 阶段免登录直进主界面；预留认证抽象接口，后期接服务器时替换实现 |
| R8 | 数据存储演进 | Demo 阶段本地 Room；当前本机服务端采用 FastAPI + SQLite，登录后文字记录以服务端为权威、Room 为缓存；照片未来接入 COS；**Demo 阶段不需要导出功能**（2026-09-16 服务端计划更新） |
| R9 | 界面形态 | 底部导航三个 Tab：**记录** / **智能规划** / **个人中心**。不做依赖地图的足迹页面；记录页按旅行日期展示内容，并在右下角提供 `+` 添加入口。城市数和出行次数统计放入后续适合的页面（第 10 轮细化；后续再次确认移除足迹页） |
| R10 | 城市添加 | 省市级联选择，内置行政区划数据（国家统计局 2023 年县及县以上行政区划，约 3000+ 城市），保证数据规范（第 3 轮确认；**第 10 轮确认数据源**） |
| R11 | Agent 扩展（远期） | 接入 Agent 做完整旅行计划推荐：综合用户画像、**预算、天气、季节**等多因素，最终**生成行程安排**；属多步工作流任务，Python 生态实现，后端内置（第 6 轮细化定案，见 4.4 节） |
| R12 | 平台与命名 | 仅安卓；App 名称「足迹（Footmarks）」，package `com.miracle.footmarks`（第 5 轮确认） |
| R13 | 推荐入口占位 | 本期 Demo 在底部导航栏增加「智能规划」Tab，实现对话界面框架（支持输入/显示消息），点击发送提示"暂未开放，待完善"；推荐逻辑待服务端 Agent 模块完成后接入（第 6 轮确认；**第 10 轮明确界面形态**） |

## 3. 待讨论问题清单

- [x] Q1: 平台范围 → 仅安卓（R12）
- [x] Q1.1: 安卓端技术栈 → 原生 Kotlin + Jetpack Compose（第 3 轮定案）
- [x] Q2: 登录方案 → Demo 免登录 + 认证接口占位（R7）
- [x] Q3: 美食是否支持照片 → 支持（R3）
- [x] Q4: 记录字段 → 名称/类型/日期/评分/花费/备注/照片（R5）
- [x] Q5: 照片存储 → Demo 本地压缩存储，后期服务器（R6/R8）
- [x] Q6: 多端同步 → Demo 单机，后期服务端化（R8）
- [x] Q7: Agent 数据结构预留 → 无标签无坐标，靠结构化字段 + 文本语义（R11，第 4 轮定案）
- [x] Q8: 城市添加方式 → 省市级联选择（R10）
- [x] Q9: 照片上限与压缩 → 9 张，压缩不保留原图（R6）
- [x] Q10: 界面形态 → 底部导航为记录/智能规划/个人中心；移除足迹页，添加入口放在记录页 `+` 按钮（R9）
- [x] Q11: 位置字段 → 砍掉（第 5 轮确认）
- [x] Q12: 标签字段 → 不做（第 4 轮确认）
- [x] Q13: App 命名 → 足迹（Footmarks），com.miracle.footmarks（R12）
- [x] Q14: Agent 框架语言/生态 → Python，工作流型 Agent，后端内置（R11，第 6 轮定案）
- [x] Q15: 本期是否动 Agent → 不实现推荐逻辑，App 预留推荐入口占位页（R13）
- [x] Q16: 数据模型架构 → 两层嵌套：旅行记录（时间段容器） + 子记录（景点/美食），同一城市可有多次旅行（第 10 轮定案）
- [x] Q17: 智能规划界面形态 → 对话框架（可输入/显示消息），点发送提示"暂未开放，待完善"（第 10 轮定案）
- [x] Q18: 城市数据源规模 → 国家统计局完整版（3000+ 含县级市）（第 10 轮定案）

## 3.1 当前 Demo 实现状态

以下内容用于说明代码现状，不改变已确认的目标需求：

| 范围 | 当前状态 | 与目标的差距 |
|---|---|---|
| 数据模型 | 已实现 `City → Trip → Record`、起止日期、日期范围约束及 Room 1 → 2 → 3 数据迁移；Trip/Record 有可空服务端 ID | 旧本地旅行不自动上传；登录时会保护旧数据 |
| 记录 CRUD | 已实现旅行及首条记录创建、向已有旅行追加记录、列表/详情查询、编辑、删除、真实城市名和输入边界校验 | API 24 发布前设备回归留在阶段 10 |
| 城市选择 | 已内置 2023 年 3429 条省/市/区县数据，支持省份筛选、中文搜索、层级路径和规范化代码 | 拼音搜索未列入当前 Demo 范围 |
| 照片 | 最多选择 9 张；按 1080px 长边、JPEG 质量 80 压缩到 App 内部目录；列表/详情可预览；编辑和删除会清理对应副本 | 手工设备回归仍需持续验证重启显示与多图操作 |
| 记录列表 | 已按 Trip 开始日期倒序展示旅行卡片和内部子记录，顶部显示城市数与出行次数 | 搜索和筛选留到后续版本 |
| 智能规划 | 已实现欢迎消息、消息区、可保留草稿的输入框和发送提示 | 真实 Agent 对话待后端阶段接入 |
| 个人中心 | 已实现统计、版本、共享账号登录和手动刷新文字记录 | 云端照片和完整多设备验收待后续阶段 |
| 服务端 | FastAPI + SQLAlchemy 2 + SQLite + Alembic；单一共享账号认证、Trip/Record CRUD、统计；Android 已接入文字同步 | COS、云服务器和 Agent 暂缓；两台真实设备与 API 24 网络回归待补 |

## 4. 技术栈讨论（最终定案）

### 4.1 选型过程

- 第 1 轮提出候选：原生 Kotlin + Jetpack Compose（仅安卓时官方首选）vs Flutter（跨端优势用不上，因无 iOS 计划）
- 第 3 轮确认用户为 Python/后端背景，由 Claude 主导开发、用户能读懂代码即可 → 无跨端需求 + 开发主体是 AI → **原生 Kotlin 定案**
- 第 3 轮曾考虑 **FastAPI + PostgreSQL**；2026-09-16 的服务端计划针对单人小应用简化为 **FastAPI + SQLite**，并已按新计划实施。

### 4.2 安卓端技术栈

| 层 | 技术 | 说明 |
|---|------|------|
| 语言 | Kotlin | 官方语言 |
| UI | Jetpack Compose + Material 3 | 声明式 UI，现代安卓标准 |
| 架构 | MVVM（ViewModel + StateFlow） | UI 与数据解耦，后期接服务端只动 Repository 层 |
| 本地存储 | Room | 官方 ORM，后期加字段走 migration |
| 图片 | Coil（加载）+ 系统 BitmapFactory（压缩） | 压缩至约 1080px 长边 JPEG |
| 路由 | Navigation Compose | 页面跳转 |
| 依赖注入 | Hilt | 当前 Demo 已接入，用于提供 Room、DAO、Repository 和 ViewModel 依赖 |
| 相册选图 | Photo Picker（系统照片选择器） | Android 13+ 原生支持，旧版本走 Activity Result 兼容 |

### 4.3 本机服务端与后续部署

- FastAPI + SQLite 已在 `server/` 实现，Android 使用 Retrofit 接入；可继续免登录使用本地 Demo，也可在无旧本地旅行时登录共享账号。
- 云端文字记录以服务端为权威，Room 通过可空服务端 ID 映射并作为缓存。云端写操作由现有页面 ViewModel 转发协调层，成功后拉取刷新；断网不产生独立本地写入。
- 本地服务端已提供 SQLite 备份/恢复命令和统一请求校验错误格式；备份文件由用户自行保存，不自动上传。
- 初始账号由服务端本地命令创建，无公开注册；原图 COS、Agent 和云服务器部署尚未实施。
- **跨语言对接原理**（第 9 轮澄清）：Kotlin（Java 生态）与 Python（FastAPI）之间走 HTTP + JSON 网络协议通信，协议语言中立（同浏览器 JS 调 FastAPI 的模型），**零桥接层**；Retrofit/序列化库只是各自生态内的常规工具（类比 httpx 与 Pydantic），不存在「Kotlin-Python 转换层」。跨语言唯一成本是 **API 契约对齐**（JSON 字段名/类型两侧一致，FastAPI 自动生成的 OpenAPI 文档为契约权威源）；进程内调用才需要生态桥接（py4j/JNI），App 与服务器永远隔网络、永不处于该情况

### 4.4 Agent 接入方案（远期，第 6 轮定案）

**需求本质**：根据用户画像 + 预算 + 天气 + 季节 → 生成完整行程安排。这是**多步工作流任务**（需调用外部天气 API、做预算约束计算、逐日编排行程），不是单轮文本分析，因此：

| 决策点 | 定案 | 理由 |
|--------|------|------|
| 语言/生态 | **Python** | 与 FastAPI 同生态；AI 框架与 SDK 断层式领先；用户可自行维护。否决 Java（Spring AI）：与服务端技术栈割裂 |
| 部署形态 | **FastAPI 内置模块**，不独立部署 | Agent 必须读库内用户数据，长在后端里最自然；独立服务部署运维成本翻倍 |
| 框架级别 | **工作流型框架**（LangGraph 或等价，实施时定） | 预算/天气/季节多因素多步骤推理超出「直调 LLM」能力；具体框架等实施时按当时生态选，不提前锁死 |
| 模型选型 | LLM SDK 层抽象，模型可替换 | 实施时再定（Claude / 国产模型等），架构上不绑死 |
| App 端边界 | **安卓端只调 REST**（`POST /api/recommend`），不直接对接 Agent | 后期换模型/换框架，App 零改动 |

**远期推荐流程设想**（实施时细化）：

1. 画像分析：读取历史记录（城市/评分/花费/备注），LLM 总结偏好（喜欢什么、预算档位、出行季节习惯）
2. 候选生成：结合当前季节 + 用户偏好生成候选目的地
3. 约束筛选：预算过滤（历史花费数据支撑估算）+ 天气因素（外部天气 API）
4. 行程编排：逐日安排景点/美食，输出结构化行程方案

**本期 Demo 落地物**：App 内已完成「智能规划」对话框架和“暂未开放，待完善”发送提示（见 6.2），数据模型已确认可支撑上述分析（第 4 轮结论）。

### 4.5 技术栈生命周期：从 Demo 到服务端（第 7 轮澄清）

核心认知：**安卓端没有一项技术是 Demo 临时替代品，后期架构「只加不换」**。

分层的汉堡模型（每层只管一件事）：

```
UI 层    Compose(画笔) / Material 3(组件样式) / Coil(图片加载) / Navigation(路由)
逻辑层   MVVM-ViewModel（UI 与数据中转，屏幕旋转数据不丢）
数据层   Repository 接口（数据门面）+ Room（本地 SQLite）
系统层   Photo Picker（系统选图，免权限）
```

各技术从 Demo 到服务端期的命运：

| 技术 | Demo 期 | 服务器期 | 命运 |
|------|--------|---------|------|
| Kotlin / Compose / Material 3 / Navigation / Coil | UI 全家桶 | 不变 | 永久 |
| MVVM 分层 | 解耦 | 不变 | 永久 |
| Room | 唯一数据源 | 降级为本地缓存（离线可看、秒开、省流量） | 保留，角色变化 |
| Repository 接口 | 本地实现 | 增加远程实现 + 同步逻辑 | 接口不变，实现加码 |
| AuthRepository | 「永远已登录」 | 接真实登录 | 替换实现 |
| Photo Picker | 系统选图 | 不变 | 永久 |

## 5. 数据模型（第 10 轮定案）

采用 **两层嵌套模型（方案 B）**：

```
City（城市）
  ├─ Trip（旅行记录1）：startDate: 2025-01-15, endDate: 2025-01-17
  │   ├─ Record（子记录）：故宫，景点，2025-01-15，5星，100元
  │   ├─ Record（子记录）：长城，景点，2025-01-16，4星，80元
  │   └─ Record（子记录）：烤鸭，美食，2025-01-15，5星，200元
  └─ Trip（旅行记录2）：startDate: 2026-03-20, endDate: 2026-03-22
      └─ Record（子记录）：故宫，景点，2026-03-20，5星，100元
```

### 5.1 数据表设计

#### City（城市表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| provinceCode | String | 省级行政区划代码 |
| cityCode | String | 市级行政区划代码 |
| cityName | String | 城市名称 |
| createdAt | Long | 创建时间戳 |

#### Trip（旅行记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| cityId | Long | 外键 → City.id |
| startDate | Long | 开始日期时间戳 |
| endDate | Long | 结束日期时间戳 |
| createdAt | Long | 创建时间戳 |

#### Record（子记录表 - 景点/美食）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| tripId | Long | 外键 → Trip.id |
| name | String | 景点/美食名称 |
| type | Enum | SPOT（景点）/ FOOD（美食） |
| date | Long | 具体日期时间戳（必须在 Trip 的时间段内） |
| rating | Int? | 评分 1-5 星，**可选** |
| cost | Float? | 花费（人民币），**可选** |
| note | String? | 备注 |
| photos | String | 照片路径列表（JSON 数组字符串，最多 9 张） |
| createdAt | Long | 创建时间戳 |

### 5.2 关键约束

1. **同一城市多次旅行**：通过创建多条 Trip 记录实现，每条 Trip 有独立的时间段
2. **日期约束**：Record.date 必须在对应 Trip 的 [startDate, endDate] 范围内
3. **照片存储**：路径格式 `photos/{tripId}/{recordId}/{timestamp}.jpg`，存储在 app internal storage
4. **删除级联**：删除 City → 删除所有 Trip → 删除所有 Record → 删除所有对应照片文件

## 6. 界面流程（第 10 轮细化）

### 6.1 底部导航结构

```
┌─────────────────────────────────────┐
│           App 主界面                 │
├─────────────────────────────────────┤
│                                     │
│         [当前 Tab 内容区域]          │
│                                     │
├─────────────────────────────────────┤
│  📍记录  |  🤖智能规划  |  👤个人中心  │
└─────────────────────────────────────┘
```

### 6.2 Tab 内容详细

#### Tab 1: 📍 记录
- **主内容**：按 Trip.startDate 倒序展示旅行记录，每个 Trip 卡片内展示景点/美食子记录
- **添加入口**：右下角 `+` 按钮
- **不包含**：足迹地图或独立足迹页面
- **统计**：城市数和出行次数显示在记录页顶部，个人中心复用统计数据

#### Tab 2: 🤖 智能规划
- **界面形态**：对话界面（类似聊天 App）
- **输入框** + **发送按钮**：点击发送时弹出 Toast 提示 "暂未开放，待完善"
- **消息展示区**：本期显示助手欢迎消息，后续接入真实消息列表
- **后期接入**：连接服务端 Agent API，实现真实推荐对话

#### Tab 3: 👤 个人中心
- 用户信息（Demo 阶段显示本地用户占位信息）
- 设置入口（预留）
- 关于页面

### 6.3 添加记录流程

```
记录 Tab → 点击"+" → 选择城市（省市级联）
  ↓
通过主 `+` 创建 Trip（输入旅行时间段：开始日期~结束日期），或通过已有 Trip 卡片内的 `+` 追加子记录
  ↓
添加子记录 → 填写景点/美食信息
  ↓  类型：景点/美食（单选）
  ↓  名称：文本输入
  ↓  日期：日期选择器（限制在 Trip 时间段内）
  ↓  评分：星级选择（可跳过）
  ↓  花费：数字输入（可跳过）
  ↓  备注：多行文本
  ↓  照片：最多 9 张（Photo Picker 选图 → 压缩存储）
  ↓
保存 → 返回记录时间线 → 可通过 Trip 卡片内的 `+` 继续添加子记录
```

### 6.4 城市数据源（第 10 轮定案）

- **数据源**：国家统计局 2023 年县及县以上行政区划代码（公开数据）
- **数据量**：约 3000+ 城市（含地级市、县级市、自治州）
- **接入方式**：下载 JSON 文件内置到 App assets 目录
- **省市联动**：省份列表 → 选择省 → 该省下所有市列表
- **数据格式示例**：
```json
{
  "provinces": [
    {
      "code": "110000",
      "name": "北京市",
      "cities": [
        {"code": "110100", "name": "北京市"}
      ]
    },
    {
      "code": "130000",
      "name": "河北省",
      "cities": [
        {"code": "130100", "name": "石家庄市"},
        {"code": "130200", "name": "唐山市"}
      ]
    }
  ]
}
```

## 7. 后续讨论待办（如有新问题追加到此）

- [ ] 个人中心具体内容细节（用户头像？昵称？统计数据？）
- [x] 时间线 Trip 卡片 → 展示城市、起止日期、记录数和内部景点/美食；按开始日期倒序
- [ ] 记录搜索范围（只搜索城市，还是同时搜索景点/美食）
- [x] Room 1 → 2 迁移 → 每条旧记录按原城市和游览日期归入一条单日 Trip，完整保留原记录
