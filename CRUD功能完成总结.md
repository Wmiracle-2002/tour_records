# Footmarks Demo：记录 CRUD 里程碑总结

日期：2026-09-15  
提交：`d8c668a Footmarks demo: complete CRUD records`  
分支：`feat/demo`，已推送到 `origin/feat/demo`

## 交付范围

| 能力 | 当前实现 |
|---|---|
| 创建 | 添加景点/美食记录，包含城市、名称、日期、可选评分、可选人民币花费、备注和照片 URI |
| 查询 | 日期倒序记录列表、单条记录详情、详情中的真实城市信息 |
| 更新 | 编辑当前记录的全部业务字段并写回 Room |
| 删除 | 详情页确认后删除数据库记录 |
| 城市 | 20 个常用城市初始化、中文名称搜索、手动创建城市 |
| 持久化 | Room 数据库、Flow 响应式刷新 |

## 用户流程

```text
记录页 → “+” → 选择/创建城市 → 填写并保存
       → 记录列表 → 记录详情 → 编辑或删除
```

底部导航为“记录 / 智能规划 / 个人中心”。其中后两个页面当前仅为占位。

## 技术实现

- Kotlin 1.9.20、Jetpack Compose、Material 3
- Room 2.6.1、Hilt 2.48
- Navigation Compose 2.7.6、Coil 2.5.0
- Kotlin Coroutines、Flow、StateFlow
- Gradle 8.4、JDK 17、compile/target SDK 34、min SDK 24

## 验证边界

本里程碑中的“CRUD 完成”表示四类操作已有代码路径且项目可以构建，不等于最终产品需求全部完成。完整设备回归和自动化测试仍待补充。

> 后续进展（2026-09-15）：阶段 1 已加入 `City → Trip → Record` 三层数据模型和 8 个 Room 仪器测试，并修复编辑时 `createdAt` 变化的问题。本节其余内容保留为提交 `d8c668a` 当时的里程碑快照。

## 未包含在本里程碑中

- `Trip` 旅行时间段实体及 `City → Trip → Record` 模型
- 3000+ 国家行政区划数据和省市级联
- 照片压缩保存、9 张限制及删除清理
- 列表中的真实城市名称
- 智能规划对话框架
- 个人中心统计
- CRUD 自动化测试

## 已知风险

- 当前照片只保存相册 URI，长期读取权限未验证。
- 手动创建城市时使用占位行政区划代码，数据规范性不足。
- 编辑记录时 `createdAt` 会使用默认值重新生成。
- 记录保存失败会显示异常消息，但缺少针对常见数据库错误的用户友好提示。

具体回归步骤见 [测试指南.md](./测试指南.md)，后续安排见 [开发计划.md](./开发计划.md)。
