# Agent 稳定性计划

更新时间：2026-09-26
状态：最小化改进 A～E 已在 `fix/agent` 提交并推送；2026-09-26 已将其与美食推荐、距离意图修复部署到服务器，并同步新模型名。容器内假 LLM/假高德冒烟测试和公网健康检查通过；新模型真实调用与 Android 真机尚未验收。

## 1. 当前目标和功能边界

首要目标是让用户发出消息后，在可接受的时间内得到**有依据的文字回答**。本项目是个人旅行记录 App；Agent 只处理以下五类问答和粗粒度行程规划：

| 功能 | 事实来源 | 回答要求 |
| --- | --- | --- |
| 去过哪里、哪些景点和美食 | 当前登录用户的 SQLite 旅行与小记录 | 按城市和记录类别回答；没有记录就明确说明，不能把高德 POI 当成去过的地方。 |
| 景点与美食推荐 | 高德 Web 服务 `/v3/place/text` | 限定城市、区分景点与餐饮；候选不足时不编造地点。 |
| 两地点距离 | 两端经核对的 POI 坐标及 `/v3/distance` | 默认标明为直线距离；明确要求驾车或短距离步行时才用对应模式。 |
| 当前或指定日期天气 | `/v3/config/district` 和 `/v3/weather/weatherInfo` | 实况与预报分开；只回答高德实际覆盖的日期。 |
| 旅行预算 | 服务端本地估算 | 输出人民币粗估及假设，不当作实时价格。 |
| 一至三日旅行规划 | 景点和餐饮 POI；可选本人历史、天气和本地预算 | 按第 N 天或已知日期，展示早餐、上午、午餐、下午、晚餐、晚上；缺少候选的时段留空。 |

当前**不提供导航路线、转弯指令、精确到分钟的行程、已核实的开放时间或实时门票/餐费**。这些数据缺失不能被 LLM 补写。天气、预算等可选信息不可用时，只要景点和餐饮候选可靠，仍可给出基本行程并说明限制。节日日期无法可靠换算或超出高德预报范围时，要求具体日期或明确说明无法查询。

## 2. 已完成的基线与待验证事实

- 原 P0 请求级观测已部署：Android、Nginx、FastAPI、Agent 阶段、LLM 和 Tool 可用 `request_id` 关联；日志记录阶段、耗时、状态和必要的业务参数，**不记录 API Key、Token、完整 Prompt 或模型隐藏推理**。
- 原 P1 的历史、天气、景点、预算和旧版路线问答曾在手机上验收；原 P2 的 ReAct 轮数上限、请求/阶段预算、客户端断开处理和超时映射已部署。这些结果只证明当时的服务器版本，不代表最新改进版已上线；旧版路线验收不能沿用为新版距离验收。
- 后续参数模型修正和最小化改进 A～E 已在 `fix/agent` 完成：高德请求前有硬校验；距离、天气和预算走确定性分支；规划分别收集景点与餐饮，按六个粗时段生成并校验，不调度路线导航工具。
- E 步用认证的 FastAPI 测试客户端模拟 Android 消息，覆盖五类问答、规划及五种无数据降级；当时服务端全量回归 **370 passed、2 warnings**。真实高德联调验证了南京行政区、景点/餐饮、天气及中山陵到夫子庙的直线测距，并发现、修复了基础 POI 响应缺少 `adcode` 时被全部过滤的问题。
- 2026-09-26 真机日志暴露旧版服务器仍把“推荐南京美食”送入 `keywords=南京景点`，把“南京中山陵到夫子庙有多远”误当路线并调用 `/v3/geocode/geo`。已部署新版：显式景点/美食由服务端核对类别并直接查询高德；“有多远”被错判为路线时转为距离问答，先查两端 POI 再测距。服务器 `.env` 只更新了模型名，原 Base URL、Key 及其他配置保留。
- 同日修复“现在/当前/此刻/目前”被模型误归类为日期预报的问题：无其他日期或范围条件时由服务端校正为实时查询，走高德 `base`；“今天全天/明天/从现在到明天”继续走 `all`。服务器容器内假 LLM 冒烟验证通过，真实模型与手机验收待完成。
- **尚无证据**证明最新版使用新模型时能在 Android 上满足响应时间与内容质量要求。服务器内假 LLM 的通过结果不能代替真实模型和手机验收；不把历史测试计数当作当前代码的新测试结果。

## 3. 当前优先级与实施顺序

每个优先级只解决该阶段的阻断问题。先通过服务端模拟请求和完整回归，再部署，最后做真实链路和手机验收；每一步记录请求 ID、HTTP 状态、总耗时、关键阶段耗时与回答是否有依据。

### P0：部署一致性与五类问答真实验收

目标：确认服务器运行的是本次 `fix/agent` 版本，真实 LLM 能稳定完成需求解析，五类问答能从手机得到正确或明确的降级回答。

1. 部署前核对本地提交、服务器代码版本、配置项和密钥是否齐全；不覆盖或清空服务器现有 `.env`，也不在日志或文档中写出密钥值。重建 API 容器，检查迁移、健康接口与 Nginx 反向代理。本次部署和健康检查已完成；新模型真实调用待用户额度可用后验收。
2. 先从服务器发送与 Android 相同的认证 `POST /api/v1/agent/chat`，逐项验证下表；再用手机发送同样的问题。每次用响应 `X-Request-ID` 对照 API 与 Nginx 日志。401 后 Token 刷新并重试应正常。
3. 若答案错误，先定位是需求解析、参数校验、Tool/高德响应、结果归一化还是最终回答；只修复已复现的阶段，不先扩大超时或增加重试。

| 场景 | 示例问题 | 通过条件 |
| --- | --- | --- |
| 历史 | `我去过南京哪些景点？`、`我去过哈尔滨哪些地方？` | 仅使用本人记录；前者按实际记录回答，后者在无记录时明确说明。 |
| 推荐 | `推荐南京的景点`、`推荐南京的美食` | 景点与餐饮分别返回经城市和类别核对的 POI；无合格候选时说明，不能编造。 |
| 距离 | `南京中山陵到夫子庙有多远？` | 两端各有唯一合格 POI；回答标明测距方式，不输出导航步骤或夸大的距离。 |
| 天气 | `南京现在天气怎么样？`、`南京明天天气怎么样？` | 实况使用 `base`，明天使用 `all` 并匹配日期；缺失目标日期时不回退到今天。 |
| 预算 | `两个人去南京三天大概要花多少钱？` | 输出人民币粗估、人数/天数及非实时说明；缺少天数时澄清。 |

通过标准：以上正常与无数据场景在服务器模拟请求和手机上均得到非空、可解释的回答；没有未定位的 502、503、504 或连接断流；认证刷新、断网后的草稿保留和重发正常。真实天气与 POI 的具体内容会随日期和高德数据变化，不把固定值写成断言。

### P1：一至三日规划的正确性与耗时

目标：稳定得到可展示的文字行程，避免重回多轮 Tool/LLM 调用造成的超时。

按一日、二日、三日依次验收，每种都测试“只给城市和天数”以及“给出公历出发日期”：

1. 需求分析能提取城市、天数、日期与限制；没给日期只显示第 N 天，不编造日期。
2. 规划分别取得景点与餐饮候选；具名地点必须引用同城合格 POI 或本人记录，类别、名称和 ID 一致。早餐/午餐/晚餐只放餐饮，上午/下午/晚上放景点；同一天不重复安排同一地点。
3. 候选不足时相应时段显示“暂无可靠推荐”；生成器输出空行程、未知 POI、错类、错日期或超时时，应受控降级，不以虚构地点填满六个时段。
4. 要求避开去过的地方时，用本人历史记录检查候选；历史不可用时明确说明未完成去重。预算上限、天气不可用均应有说明，不阻断已有可靠地点的基本行程。
5. 对每次请求记录总耗时以及 Requirement Analyzer、POI 请求、Itinerary Generator、Validator、Reviser 的耗时与次数；规划不能再为已确定的 POI 和预算反复调用 ReAct。若接近预算，先减少冗余调用和输出规模，再评估异步任务。

通过标准：一日游通过后才进入二日、三日；正常请求在当前 **Agent 总预算 120 秒、Android 请求预算 150 秒**内完成，且 Nginx 等待时间大于 Agent 总预算；失败时在客户端预算内返回明确结果，不产生无提示断流。以上数值以部署配置核对为准，不能仅凭本地默认值认定线上一致。

### P2：边界、回归与内容可信度

目标：让异常输入和外部数据故障只影响对应功能，不污染其他回答。

- 覆盖同名异地、错城 POI、缺少 `adcode` 但有精确城市名、无坐标、重复 ID、高德错误码、非法距离参数；错误请求在发往高德前被拒绝。
- 覆盖“此刻、今天、今晚、明天、后天、从现在到明天、节日”的时间分类，以及节日年份不明、日期超出预报、预报缺天和 adcode 不匹配。
- 覆盖无历史、无候选、预算缺少天数、LLM 不合规 JSON、Tool 超时、客户端断开、401 刷新；每种情况都不编造本人历史、地点、天气、开放时间或价格。
- 跑 Agent API、工具边界与服务端全量回归；有 Android 改动时再跑 JVM 与设备测试。实际高德返回结构变动时，先补复现测试，再修改过滤规则。

通过标准：相关边界测试和全量回归通过；对外错误有明确说明，日志可通过 `request_id` 找到失败阶段；修复不重新引入路线导航或精确时间安排。

### P3：长任务体验，按证据决定

只有 P1 的真实规划请求在减少冗余调用后仍经常超过同步预算，才评估异步任务、进度查询或流式输出。届时先确定 Android 展示方式、服务端任务状态与取消行为，再实施；不为当前版本预先引入任务队列。多轮记忆、更开放的闲聊、供应商切换和复杂工具编排继续排在稳定验收之后。

## 4. 当前请求链路与排查顺序

```text
Android → Retrofit/OkHttp → Nginx → FastAPI 认证与 request_id
  → AgentRuntime → Requirement Analyzer（LLM）
     ├─ 历史/POI 推荐：ReAct 收集 → 结构化事实回答
     ├─ 距离/天气/预算：服务端固定参数与调用顺序 → 事实回答
     └─ 行程：景点 POI + 餐饮 POI + 可选历史/预算
              → Itinerary Generator（LLM）→ Validator
              → 必要时 Reviser 或受控降级 → 六时段回答
  → FastAPI JSON → Nginx → Android 显示
```

一次请求先查 Android 显示的状态和 `X-Request-ID`，再按同一 ID 查看 Nginx、API 阶段事件、LLM 和高德请求。扫描器访问 `/`、`/wp-admin` 等产生的 404 与 Agent 请求无关。状态码定位：401 先看 Token 刷新；499 看客户端是否先断开；504 看 Nginx/Agent/LLM 哪层先耗尽预算；502 看上游或结构化校验；503 看配置；HTTP 200 但内容不对则检查需求、POI/天气事实和最终回答。不能只凭状态码断定原因。

服务器观察命令：

```bash
cd /opt/footmarks/server
sudo docker compose logs -f --tail=0 api
```

另开窗口查看 Nginx 请求 ID 与状态：

```bash
sudo tail -f /var/log/nginx/access.log /var/log/nginx/error.log
```

## 5. 每一步的开发闭环

1. 写清要解决的一个问题、可观察的成功标准和请求样例。
2. 用模拟 Android 的认证 API 请求或边界测试复现；记录当前 `request_id` 和失败阶段。
3. 修改最小范围的代码，优先使用参数模型、服务端固定规则和响应校验等硬约束；Prompt 示例只作为辅助。
4. 跑定向测试与服务端全量回归；检查 `git diff --check`，保留既有无关工作区改动。
5. 部署对应提交，不覆盖 `.env`；核对容器、健康检查和实际配置；再做真实 LLM/高德与 Android 验收。
6. 在开发日志、README 和本计划中记录日期、提交/部署版本、实测结果、耗时、未通过项及下一步。只有真实验收通过，才把状态写为“已上线稳定”。
