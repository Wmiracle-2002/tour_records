# 足迹记录 App 测试指南

## 测试环境要求

### 硬件要求
- Android 真机或模拟器
- 最低版本：Android 7.0 (API 24)
- 目标版本：Android 14 (API 34)
- 存储空间：至少 100MB

### 软件要求
- Android Studio (推荐最新稳定版)
- JDK 17
- Android SDK

---

## 快速测试步骤

### 方法 1：使用 Android Studio（推荐）

#### 1. 打开项目
```bash
# 在 Android Studio 中打开项目目录
File -> Open -> 选择 G:\Program Files\tour_records
```

#### 2. 连接设备
**选项 A：使用真机**
1. 在 Android 手机上启用"开发者选项"
   - 设置 -> 关于手机 -> 连续点击"版本号" 7次
2. 启用 USB 调试
   - 设置 -> 开发者选项 -> USB 调试
3. 用 USB 线连接电脑
4. 手机上允许 USB 调试授权

**选项 B：使用模拟器**
1. 在 Android Studio 中打开 Device Manager
2. 点击 "Create Device"
3. 选择设备型号（推荐 Pixel 6）
4. 选择系统镜像（推荐 API 34 或 API 30）
5. 点击 Finish 创建模拟器
6. 启动模拟器

#### 3. 运行应用
1. 在 Android Studio 顶部工具栏选择你的设备
2. 点击绿色三角形 "Run" 按钮（或按 Shift + F10）
3. 等待编译和安装（首次约 2-3 分钟）
4. 应用自动启动

---

### 方法 2：使用命令行

#### 1. 编译 APK
```bash
cd "G:/Program Files/tour_records"
./gradlew assembleDebug
```

编译成功后，APK 位置：
```
app/build/outputs/apk/debug/app-debug.apk
```

#### 2. 安装到设备
**使用 ADB 安装：**
```bash
# 查看已连接设备
adb devices

# 安装 APK
adb install app/build/outputs/apk/debug/app-debug.apk

# 如果已安装，需要先卸载或使用 -r 参数覆盖安装
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

#### 3. 启动应用
```bash
adb shell am start -n com.miracle.footmarks/.MainActivity
```

---

## 功能测试清单

### 测试 1：应用启动和导航 ✅

**测试步骤**：
1. 启动应用
2. 观察底部导航栏是否显示 3 个图标
3. 依次点击 3 个 Tab：时间线、足迹、添加

**预期结果**：
- ✅ 应用成功启动，无崩溃
- ✅ 底部导航栏显示 3 个图标
- ✅ 点击 Tab 可以切换页面
- ✅ 时间线显示空状态提示："还没有记录，快去添加吧"
- ✅ 足迹页面显示占位文本："足迹页面 - 待实现"

---

### 测试 2：添加记录功能 ✅

**测试步骤**：
1. 点击底部导航栏的"添加"图标
2. 选择记录类型（景点或美食）
3. 点击"选择城市"，在弹窗中点击"确定"（临时会选择北京）
4. 输入名称，例如："故宫"
5. 点击日期选择框，选择日期
6. 点击评分选择（例如 5 星）
7. 输入花费，例如："60"
8. 输入备注，例如："天气很好"
9. 点击"添加照片"按钮，从相册选择一张照片
10. 点击顶部的"保存"按钮

**预期结果**：
- ✅ 所有表单字段正常显示
- ✅ 日期选择器弹窗正常显示
- ✅ 相册选择器正常打开
- ✅ 照片显示在列表中
- ✅ 点击保存后返回时间线页面
- ✅ 数据保存到数据库

**已知问题**：
- ⚠️ 城市选择器是占位实现，固定选择"北京"
- ⚠️ 照片未实际压缩存储（仅保存 URI）

---

### 测试 3：查看时间线 ✅

**测试步骤**：
1. 添加至少 2 条记录（一条景点、一条美食）
2. 切换到"时间线" Tab
3. 观察记录列表

**预期结果**：
- ✅ 记录按日期倒序显示（最新的在上面）
- ✅ 每条记录显示：
  - 照片预览（或类型图标）
  - 类型标签（景点/美食，颜色不同）
  - 名称
  - 城市名称（当前显示"城市名称"占位文本）
  - 日期（格式：yyyy-MM-dd）
  - 评分（如果有）
- ✅ 可以滚动列表

**已知问题**：
- ⚠️ 城市名称显示为占位文本，未关联查询

---

### 测试 4：照片功能 📸

**测试步骤**：
1. 进入"添加记录"页面
2. 点击"添加照片"按钮
3. 选择一张照片
4. 观察照片预览
5. 点击照片右上角的 "X" 删除照片
6. 再次添加照片
7. 保存记录
8. 在时间线查看该记录

**预期结果**：
- ✅ 相册选择器正常打开
- ✅ 照片显示为缩略图（100x100）
- ✅ 点击删除按钮可以移除照片
- ✅ 可以添加多张照片
- ✅ 时间线显示第一张照片作为预览

**已知问题**：
- ⚠️ 照片未压缩，直接保存原始 URI
- ⚠️ 如果照片来自外部，应用重启后可能无法加载

---

### 测试 5：数据持久化 💾

**测试步骤**：
1. 添加 3-5 条记录
2. 完全关闭应用（从后台清除）
3. 重新打开应用
4. 切换到时间线页面

**预期结果**：
- ✅ 应用重启后数据仍然存在
- ✅ 时间线显示之前添加的所有记录
- ✅ 数据顺序保持不变

---

### 测试 6：边界条件测试 ⚠️

**测试场景**：
1. **必填项验证**：
   - 不选择城市，直接点击保存
   - 不输入名称，直接点击保存
   - 预期：显示错误提示

2. **空数据显示**：
   - 首次安装，时间线无数据
   - 预期：显示"还没有记录，快去添加吧"

3. **长文本输入**：
   - 名称输入超长文本（100+ 字符）
   - 备注输入超长文本（1000+ 字符）
   - 预期：正常保存和显示

4. **特殊字符**：
   - 名称包含 emoji 😊
   - 备注包含换行符
   - 预期：正常保存和显示

---

## 性能测试

### 1. 启动时间
- 冷启动时间应 < 3 秒
- 热启动时间应 < 1 秒

### 2. 列表滚动
- 添加 50+ 条记录后测试
- 滚动应流畅，无明显卡顿
- 使用 LazyColumn 实现虚拟化滚动

### 3. 照片加载
- 多张照片加载应异步进行
- 使用 Coil 库自动缓存

---

## 调试技巧

### 查看日志
```bash
# 查看应用日志
adb logcat | grep Footmarks

# 查看崩溃日志
adb logcat | grep AndroidRuntime
```

### 查看数据库
```bash
# 进入应用数据目录
adb shell
cd /data/data/com.miracle.footmarks/databases/

# 导出数据库到电脑
adb pull /data/data/com.miracle.footmarks/databases/footmarks_db /path/to/save/
```

使用 [DB Browser for SQLite](https://sqlitebrowser.org/) 打开数据库文件查看内容。

### 清除应用数据
```bash
# 清除应用数据（重置到首次安装状态）
adb shell pm clear com.miracle.footmarks
```

---

## 常见问题排查

### 问题 1：应用安装失败
**可能原因**：
- 设备上已有同包名应用（不同签名）
- 存储空间不足

**解决方法**：
```bash
# 卸载旧版本
adb uninstall com.miracle.footmarks

# 重新安装
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 问题 2：应用崩溃
**排查步骤**：
1. 查看 logcat 日志
2. 检查是否有权限问题（存储、相机）
3. 检查数据库迁移是否正确

### 问题 3：照片无法加载
**可能原因**：
- 照片 URI 权限过期
- 外部存储路径变化

**临时解决**：
- 重新添加照片
- 使用内部存储复制照片

### 问题 4：编译失败
**常见原因**：
- Gradle 下载超时
- JDK 版本不匹配

**解决方法**：
```bash
# 清理项目
./gradlew clean

# 重新编译
./gradlew assembleDebug
```

---

## 自动化测试（可选）

### 单元测试
```bash
# 运行单元测试
./gradlew test
```

### UI 测试（待实现）
```bash
# 运行 Instrumentation 测试
./gradlew connectedAndroidTest
```

---

## 测试报告模板

### 测试环境
- 设备型号：_____________
- Android 版本：_____________
- 测试日期：_____________

### 测试结果
| 功能 | 状态 | 备注 |
|------|------|------|
| 应用启动 | ✅/❌ | |
| 底部导航 | ✅/❌ | |
| 添加记录 | ✅/❌ | |
| 时间线显示 | ✅/❌ | |
| 照片上传 | ✅/❌ | |
| 数据持久化 | ✅/❌ | |

### 发现的问题
1.
2.
3.

---

## 下一步测试计划

当以下功能开发完成后，需要新增测试：

1. ✅ **城市选择器测试**
   - 搜索功能
   - 城市列表滚动
   - 选中城市

2. ✅ **足迹页面测试**
   - 城市统计显示
   - 点击城市查看详情

3. ✅ **记录详情页测试**
   - 查看完整信息
   - 编辑功能
   - 删除功能

4. ✅ **照片压缩测试**
   - 验证压缩效果
   - 验证存储位置
