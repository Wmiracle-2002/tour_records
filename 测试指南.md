# 足迹 App 测试指南

更新日期：2026-09-15

## 当前测试范围

可以测试：底部页面切换、城市搜索/创建、记录新增、列表查询、详情查询、编辑、删除和 Room 持久化；Trip 数据层已有模拟器自动化测试。

暂不能按最终需求验收：Trip 时间段录入和卡片界面、数据库迁移、3000+ 城市、省市级联、照片压缩与清理、智能规划对话框架、个人中心统计、列表真实城市名。

## 环境

- JDK 17
- Android SDK Platform 34、Build Tools 34.0.0、Platform Tools
- Android 7.0（API 24）或更高版本的真机/模拟器

Android Studio 可选。使用 VSCode 和命令行时，只要环境变量和 SDK 已配置即可。

## 构建

在项目根目录执行：

```powershell
.\gradlew.bat build
```

只生成 Debug APK：

```powershell
.\gradlew.bat assembleDebug
```

APK 路径：`app/build/outputs/apk/debug/app-debug.apk`。

## 安装和启动

```powershell
adb devices
adb install -r app\build\outputs\apk\debug\app-debug.apk
adb shell am start -n com.miracle.footmarks/.MainActivity
```

如果旧版本签名不一致：

```powershell
adb uninstall com.miracle.footmarks
adb install app\build\outputs\apk\debug\app-debug.apk
```

卸载会删除本地 Room 数据。需要保留测试数据时使用 `adb install -r`。

当前数据库从版本 1 升至版本 2，尚未提供迁移脚本。旧版 App 使用 `adb install -r` 覆盖安装后，首次打开也会重建数据库并清除旧记录；这是本阶段明确排除数据库迁移后的 Demo 行为。

## CRUD 回归用例

### 1. 启动与导航

步骤：

1. 启动 App。
2. 依次点击“记录”“智能规划”“个人中心”。
3. 回到“记录”。

预期：

- App 不崩溃，默认进入“记录”。
- 三个 Tab 均可切换。
- 智能规划和个人中心显示待实现占位文字。
- 记录页右下角显示 `+`。

### 2. 城市选择

步骤：

1. 点击 `+`，打开城市选择器。
2. 搜索“北京”并选择。
3. 返回后再次打开，输入一个预置列表没有的城市名称并创建。

预期：

- “北京”可被搜索并选中。
- 新城市创建后可以用于保存记录。
- 不再出现此前由无效城市 ID 触发的保存失败。

说明：当前只支持中文名称包含匹配，没有拼音搜索或省市级联。

### 3. 新增记录

步骤：

1. 选择“景点”和一个城市。
2. 名称填写“故宫”，选择日期，评分 5，花费 60，填写备注。
3. 可选一张照片。
4. 点击保存。

预期：

- 城市和名称为空时不允许保存，并显示提示。
- 有效数据保存后返回记录列表。
- 新记录出现在列表中。

### 4. 列表与详情查询

步骤：

1. 再新增一条不同日期的美食记录。
2. 检查列表顺序。
3. 点击任一记录进入详情。

预期：

- 列表按日期倒序排列。
- 卡片显示类型、名称、日期、评分及首张照片或类型图标。
- 列表城市名当前显示占位文字“城市名称”，这是已知限制。
- 详情页显示记录完整字段和真实城市名。

### 5. 编辑记录

步骤：

1. 在详情页点击编辑按钮。
2. 修改类型、城市、名称、日期、评分、花费或备注中的至少两项。
3. 保存并返回详情或列表重新查看。

预期：修改后的字段已持久化，列表排序会随日期变化。

编辑保存会在同一事务中更新 Record 与其单日 Trip，并保留原 `createdAt`。

### 6. 删除记录

步骤：

1. 在详情页点击删除。
2. 先取消一次，确认记录仍存在。
3. 再次删除并确认。

预期：取消不修改数据；确认后返回列表，记录消失。

说明：当前只删除数据库记录，不负责删除相册原图或内部照片文件。

### 7. 数据持久化

步骤：

1. 保留至少一条记录。
2. 从最近任务中移除 App，再重新启动。
3. 如条件允许，重启设备后再次检查。

预期：Room 中的城市和记录仍存在。

照片 URI 的长期访问权限尚未专门处理，重启后照片是否可显示需要重点记录。

## 边界测试

- 不选城市保存。
- 名称为空或只有空格。
- 评分留空、花费留空。
- 花费输入 0、小数和较大数值。
- 名称和备注包含中文、换行和 emoji。
- 连续添加多张照片。目前没有 9 张限制，应作为缺陷记录。
- 连续快速点击保存，观察是否产生重复记录。
- 添加 50 条记录后滚动并检查排序。

## 日志与重置

```powershell
# Windows PowerShell 中查看关键日志
adb logcat -d | Select-String 'AndroidRuntime|com.miracle.footmarks|SQLite'

# 清空应用数据，回到首次启动状态
adb shell pm clear com.miracle.footmarks
```

失败码或崩溃出现时，记录：操作步骤、屏幕提示、设备 Android 版本，以及上述 logcat 输出。

## 自动化测试

`TripDataLayerTest` 使用内存 Room 数据库覆盖 8 个用例：Trip DAO/Repository CRUD、Record Repository CRUD、按城市查询、单日 Trip 兼容创建、事务更新、日期范围校验和城市级联删除。

先构建 App 和测试 APK：

```powershell
.\gradlew.bat assembleDebug assembleDebugAndroidTest
```

标准连接设备测试命令：

```powershell
.\gradlew.bat connectedDebugAndroidTest
```

如果 Windows 环境提示 `Failed to receive the UTP test results`，先检查日志中是否有 `Cannot generate directories for path: /tmp`。当前 Android Gradle 插件会把该路径解析到当前盘符根目录；以本项目为例，需要先创建 `G:\tmp`，再重试标准命令。

```powershell
New-Item -ItemType Directory -Path 'G:\tmp' -Force
```

仍需绕过 UTP 排查时，可安装同一批 APK 并直接调用 runner：

```powershell
adb install -r app\build\outputs\apk\debug\app-debug.apk
adb install -r app\build\outputs\apk\androidTest\debug\app-debug-androidTest.apk
adb shell am instrument -w -r -e class com.miracle.footmarks.data.TripDataLayerTest com.miracle.footmarks.test/androidx.test.runner.AndroidJUnitRunner
```

成功结果应显示 `OK (8 tests)`。这些用例验证数据层，不替代页面 CRUD 手工回归。

## 测试记录模板

| 项目 | 结果 |
|---|---|
| 日期 | YYYY-MM-DD |
| 设备 / 模拟器 |  |
| Android 版本 |  |
| 构建提交 |  |
| Gradle build | 通过 / 失败 |
| 启动与导航 | 通过 / 失败 |
| 城市选择 | 通过 / 失败 |
| 新增记录 | 通过 / 失败 |
| 列表与详情 | 通过 / 失败 |
| 编辑记录 | 通过 / 失败 |
| 删除记录 | 通过 / 失败 |
| 重启持久化 | 通过 / 失败 |
| 照片重启后显示 | 通过 / 失败 |

问题详情请写入 [开发日志.md](./开发日志.md)，包含复现步骤和日志。
