# 足迹 App 测试指南

更新日期：2026-09-17

## 当前测试范围

可以测试：底部页面切换、3429 条离线地区、省市筛选与搜索、旅行时间段、Trip 卡片、统计、子记录 CRUD、Room 1 → 2 → 3 迁移、本地照片和可选服务端文字记录同步。

纯本地 Demo 仍可独立使用。服务端账号登录、文字记录同步已进入本机联调；COS 照片、Agent 与云服务器暂缓。

## 环境

- JDK 17
- Android SDK Platform 34、Build Tools 34.0.0、Platform Tools
- Android 7.0（API 24）或更高版本的真机/模拟器

Android Studio 可选。使用 VSCode 和命令行时，只要环境变量和 SDK 已配置即可。

## 构建

在项目根目录执行：

```powershell
.\gradlew.bat build
```

只生成 Debug APK：

```powershell
.\gradlew.bat assembleDebug
```

APK 路径：`app/build/outputs/apk/debug/app-debug.apk`。

## 安装和启动

```powershell
adb devices
adb install -r app\build\outputs\apk\debug\app-debug.apk
adb shell am start -n com.miracle.footmarks/.MainActivity
```

如果旧版本签名不一致：

```powershell
adb uninstall com.miracle.footmarks
adb install app\build\outputs\apk\debug\app-debug.apk
```

卸载会删除本地 Room 数据。需要保留测试数据时使用 `adb install -r`。

当前数据库版本为 3。使用 `adb install -r` 从版本 1 覆盖升级时，迁移会把每条旧记录保留为一条同日期的单日旅行；版本 2 → 3 增加可空服务端 ID，不删除旧记录和照片。

## CRUD 回归用例

### 1. 启动与导航

步骤：

1. 启动 App。
2. 依次点击“记录”“智能规划”“个人中心”。
3. 回到“记录”。

预期：

- App 不崩溃，默认进入“记录”。
- 三个 Tab 均可切换。
- 智能规划显示对话框架；个人中心显示本地用户、旅行统计和版本信息。
- 记录页右下角显示 `+`。

### 2. 城市选择

步骤：

1. 点击 `+`，打开地区选择器。
2. 横向切换省份，确认列表随省份变化。
3. 搜索“昆山”，选择“江苏省 · 苏州市 · 昆山市”。
4. 分别搜索并选择自治州、直辖市区县样本。

预期：

- 省份列表包含中国大陆 31 个省、自治区和直辖市。
- “昆山市”等城市或区县可被搜索并选中，列表显示完整层级路径。
- 同名区县按代码区分，不会合并成同一条城市记录。
- 不再出现此前由无效城市 ID 触发的保存失败。

说明：当前支持中文名称包含匹配，不支持拼音搜索；离线数据版本与来源见 [城市数据源.md](./城市数据源.md)。

### 3. 新建旅行与首条记录

步骤：

1. 点击记录页右下角主 `+`，选择“景点”和一个城市。
2. 选择旅行开始、结束日期；名称填写“故宫”，记录日期选在旅行范围内，评分 5，花费 60，填写备注。
3. 可选择照片；连续选择时每条记录最多 9 张。
4. 点击保存。

预期：

- 城市和名称为空时不允许保存，并显示提示。
- 结束日期早于开始日期，或记录日期不在旅行范围内时显示提示。
- 有效数据保存后返回记录列表，顶部城市数和出行次数更新。
- 新记录出现在对应 Trip 卡片中。

### 4. 追加记录、列表与详情查询

步骤：

1. 点击 Trip 卡片内的 `+`，向同一次旅行新增一条美食记录。
2. 再通过主 `+` 创建同城不同时间段的第二次旅行，检查卡片顺序和统计。
3. 点击任一记录进入详情。

预期：

- Trip 卡片按开始日期倒序，卡片内记录按实际游览日期排列。
- 同城两次旅行只增加出行次数，不重复增加城市数。
- 卡片显示类型、名称、日期、评分及首张照片或类型图标。
- 列表和详情页均显示记录关联的真实城市名称。
- 详情页显示记录完整字段和真实城市名。

### 5. 编辑记录

步骤：

1. 在详情页点击编辑按钮。
2. 修改类型、城市、名称、日期、评分、花费或备注中的至少两项，并替换或移除一张照片。
3. 保存并返回详情或列表重新查看。

预期：修改后的字段已持久化，列表排序会随日期变化，移除的内部照片副本会被清理。

编辑保存会在同一事务中更新 Record 与 Trip，并保留原 `createdAt`。多日旅行中的记录日期只能位于编辑页显示的旅行时间范围内。

### 6. 删除记录

步骤：

1. 在详情页点击删除。
2. 先取消一次，确认记录仍存在。
3. 再次删除并确认。

预期：取消不修改数据；确认后返回列表，记录消失。删除 Trip 中最后一条记录后，对应空 Trip 同步删除，出行统计减少。

说明：删除记录会同步删除 App 内部保存的压缩照片，不会删除用户相册中的原图。

### 7. 数据持久化

步骤：

1. 保留至少一条记录。
2. 从最近任务中移除 App，再重新启动。
3. 如条件允许，重启设备后再次检查。

预期：Room 中的城市和记录仍存在。

照片已复制到 App 内部持久目录，不依赖相册 URI 的长期访问权限；重启后应继续显示。

### 8. 智能规划框架

步骤：

1. 进入“智能规划”，确认显示助手欢迎消息。
2. 在输入框填写普通文本和多行长文本。
3. 点击“发送”。
4. 切换到其他 Tab 后再返回。

预期：

- 页面显示消息区、输入框和发送按钮。
- 点击发送显示“暂未开放，待完善”，输入草稿不被清空。
- 返回智能规划页后草稿仍然存在。
- 整个过程不依赖网络。

### 9. 个人中心统计

步骤：

1. 清空应用数据后进入“个人中心”。
2. 确认城市、出行和总花费均为 0。
3. 创建两次同城旅行，并为子记录填写不同花费。
4. 返回个人中心检查统计，再编辑花费和删除一条记录。

预期：

- 同城两次旅行显示 1 个城市、2 次出行。
- 总花费等于所有非空记录花费之和，按人民币显示两位小数。
- 编辑和删除记录后统计自动刷新；删除最后一条子记录时出行次数相应减少。
- 页面显示本地存储说明和当前应用版本。

## 本机服务端文字同步（阶段 15）

1. 参照 [README.md](./README.md) 在 `server` 目录完成依赖安装、迁移、共享账号初始化，启动 `127.0.0.1:8000` 的 FastAPI；模拟器浏览 `http://10.0.2.2:8000/api/v1/health` 应返回 `ok`。
2. 使用**无旧本地旅行**的模拟器安装 Debug APK。在“个人中心 → 共享账号”输入用户名 `shared` 和初始化的密码，点击“登录并同步”；返回记录页查看旅行和统计。旧本地旅行存在时会提示拒绝登录，不会删除或上传旧数据。
3. 在 App 新建不带照片的旅行与子记录，编辑名称、日期、评分、花费，再删除最后一条记录；每一步重新从服务端 `GET /api/v1/trips` 核对。云端模式选择照片保存会提示暂未开放，不会假装完成同步。
4. 从另一个已登录的 API 会话修改服务端记录，在模拟器个人中心点击“刷新共享记录”，记录页应显示更新；断开服务端后仍能浏览已缓存记录，但云端写入/刷新会显示错误。
5. 重启 App 后再次进入个人中心，确认共享账号状态仍在；重启服务端后继续刷新。API 24 模拟器自动化回归已通过，两台真实 Android 设备、真实服务端弱网和 API 24 手工同步留待后续验收。

模拟器默认使用 `10.0.2.2:8000`；真机需在构建 Debug APK 时传入 `-PfootmarksApiBaseUrl=https://你的可达地址/`。不要通过公网明文 HTTP 传输账号密码。正式 Release 构建也需配置 HTTPS 地址。

## 边界测试

- 不选城市保存。
- 名称为空或只有空格。
- 评分留空、花费留空。
- 花费输入 0、小数和较大数值。
- 名称和备注包含中文、换行和 emoji。
- 连续添加照片，达到 9 张后添加入口应隐藏。
- 连续快速点击保存，观察是否产生重复记录。
- 旅行结束日期早于开始日期。
- 子记录日期早于旅行开始或晚于旅行结束。
- 同一城市创建两个不同时段的 Trip，城市数保持不变、出行次数增加。
- 添加 50 条记录后滚动并检查排序；自动化对应 `LargeDatasetTest`，会写入 60 个旅行和子记录并滚动到最旧记录。

## 日志与重置

```powershell
# Windows PowerShell 中查看关键日志
adb logcat -d | Select-String 'AndroidRuntime|com.miracle.footmarks|SQLite'

# 清空应用数据，回到首次启动状态
adb shell pm clear com.miracle.footmarks
```

失败码或崩溃出现时，记录：操作步骤、屏幕提示、设备 Android 版本，以及上述 logcat 输出。

## 自动化测试

设备测试共 34 个，覆盖原有本地流程、Room 迁移/缓存合并/云端协调层/共享账号控件、60 条记录滚动和服务端不可用时保留缓存；清单测试限制系统权限为 `INTERNET`。Android JVM 测试共 8 个，覆盖输入和日期校验、Retrofit 请求与 Token 刷新。服务端 `py -3.12 -m pytest -q` 当前 14 个测试，覆盖迁移、CRUD、认证和两会话数据一致性。

先构建 App 和测试 APK：

```powershell
.\gradlew.bat assembleDebug assembleDebugAndroidTest
```

标准连接设备测试命令：

```powershell
.\gradlew.bat connectedDebugAndroidTest
```

如果 Windows 环境提示 `Failed to receive the UTP test results`，先检查日志中是否有 `Cannot generate directories for path: /tmp`。当前 Android Gradle 插件会把该路径解析到当前盘符根目录；以本项目为例，需要先创建 `G:\tmp`，再重试标准命令。

```powershell
New-Item -ItemType Directory -Path 'G:\tmp' -Force
```

仍需绕过 UTP 排查时，可安装同一批 APK 并直接调用 runner：

```powershell
adb install -r app\build\outputs\apk\debug\app-debug.apk
adb install -r app\build\outputs\apk\androidTest\debug\app-debug-androidTest.apk
adb shell am instrument -w -r -e class com.miracle.footmarks.data.TripDataLayerTest com.miracle.footmarks.test/androidx.test.runner.AndroidJUnitRunner
```

成功结果应显示共 34 个设备测试通过。自动化测试不替代页面 CRUD 和真实服务端手工回归。

当前项目已在 API 34 `Pixel_6` 和 API 24 `Pixel_2_API24` 模拟器通过 34 个仪器测试。最低支持 API 24；API 24 的真实服务端同步、照片选择手工回归和真实设备测试仍需补做。

## 测试记录模板

| 项目 | 结果 |
|---|---|
| 日期 | YYYY-MM-DD |
| 设备 / 模拟器 |  |
| Android 版本 |  |
| 构建提交 |  |
| Gradle build | 通过 / 失败 |
| 启动与导航 | 通过 / 失败 |
| 城市选择 | 通过 / 失败 |
| 新增记录 | 通过 / 失败 |
| 列表与详情 | 通过 / 失败 |
| 编辑记录 | 通过 / 失败 |
| 删除记录 | 通过 / 失败 |
| 重启持久化 | 通过 / 失败 |
| 照片重启后显示 | 通过 / 失败 |
| 智能规划输入与提示 | 通过 / 失败 |
| 个人中心统计与版本 | 通过 / 失败 |

问题详情请写入 [开发日志.md](./开发日志.md)，包含复现步骤和日志。
