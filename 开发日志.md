# 足迹 App 开发日志

## 2026-09-18：修复线上图片接口未部署导致 404

- 原因：本地已经完成阶段 14 图片接口，但服务器正在运行的旧容器没有注册 `/api/v1/records/{record_id}/images` 路由，因此图片请求返回 `404 Not Found`。
- 已重新部署服务端 `app/`、Alembic 和依赖配置，保留服务器 `.env` 与 `data/`，重建并重启 `server-api-1` 容器。
- 回归结果：运行中的容器已注册图片上传、查询和删除接口；公网 HTTPS 健康接口返回正常，上传日志出现 `201 Created`。

## 2026-09-18：修复 COS 原图上传 413

- 原因：Nginx 默认请求体限制约为 1 MB，Android 上传原始照片时在到达 FastAPI 前被 Nginx 拒绝，返回 `413 Request Entity Too Large`。
- 已备份 `/etc/nginx/sites-enabled/footmarks` 并移至 `/etc/nginx/backups/`，将 HTTPS 代理的 `client_max_body_size` 调整为 `50m`，执行 `nginx -t` 通过且无重复 `server_name` 警告，随后热重载；公网 HTTPS 健康接口继续返回正常状态。
- 已用约 2 MB 的 multipart 请求验证公网请求可以到达 API 路由，不再被 Nginx 以 413 拦截。服务器仍需配置 COS 密钥；重新进入 App 后应测试单张大图片和 9 张图片上传。

## 2026-09-18：修复共享记录刷新空值崩溃

- 修复旧服务端或异常响应缺少 `images`、返回 `images: null` 时，Gson 将 Kotlin 非空列表解析为运行时 `null`，导致 `collectionSizeOrDefault` 崩溃的问题。
- `CloudCache` 现在兼容空的图片列表和记录列表；服务端暂时不返回图片字段时保留已有图片缓存，服务端明确返回空列表时清空图片缓存。
- 新增空图片字段回归用例；`:app:compileDebugKotlin`、`:app:compileDebugAndroidTestKotlin`、`:app:testDebugUnitTest` 通过。连接设备测试仍需在有 Android 设备时执行。

## 2026-09-18：阶段 14 COS 原图与阶段 15 Android 照片同步

- 服务端接入对象存储抽象层和 `cos-python-sdk-v5`，默认 COS 桶为 `footmark-1489262329`、区域为 `ap-hongkong`，使用用户提供的默认域名；新增图片上传、列表、删除接口，返回 COS 预签名地址，Record/Trip 删除时清理 COS 对象，单条记录最多 9 张并使用 UUID Object Key。
- Android Room 升至版本 4，增加远端图片 ID/URL 缓存及 3→4 迁移；新增 Retrofit 图片接口。新增和编辑记录上传系统选择器返回的原始文件，1080px 长边/JPEG 质量 80 副本继续只用于本地预览；列表、详情和编辑页支持 Coil 加载远端图片。
- 回归结果：服务端 pytest 24/24；`:app:compileDebugKotlin`、`:app:compileDebugAndroidTestKotlin`、`:app:testDebugUnitTest` 通过，Android MockWebServer 覆盖 1 张和 9 张原图上传。已将服务端代码、COS 依赖和 Compose 环境变量映射部署到 VPS，容器健康接口正常；`connectedDebugAndroidTest` 因当前电脑没有连接 Android 设备未执行，真实 COS 密钥、线上上传、真实设备和两台设备一致性待补。

## 2026-09-17：阶段 18 测试完善

- 新增 `LargeDatasetTest`，通过真实 `RecordsScreen` 写入 60 个 Trip/Record，验证最新记录加载和滚动到最旧记录；修正小屏幕 Profile 测试在版本信息断言前缺少滚动的问题。
- 新增服务端返回 503 时的 Android 回归，确认刷新失败不会清空已缓存的云端文字记录。
- API 34 `Pixel_6` 和 API 24 `Pixel_2_API24` 模拟器完整仪器测试均为 34/34；服务端 pytest 18/18，Android JVM 8/8。API 24 真实服务端同步、第二台真实设备和弱网环境仍待手工验收。

## 2026-09-17：本地服务端备份与错误处理

- 新增 `server/app/backup.py`，提供 SQLite 备份/恢复 CLI；操作前要求停止服务，执行完整性和应用表结构校验，使用临时文件和原子替换保护现有数据库。
- 新增备份恢复回归，覆盖备份还原、无效数据库拒绝、内存数据库拒绝和恢复失败不替换原数据库；服务端 pytest 从 17 个增加到 18 个并全部通过。
- FastAPI 新增统一请求校验错误格式，响应只返回字段位置和消息，不回显输入内容；README 和测试指南补充本地操作步骤。

## 2026-09-17：阶段 15 本机同步回归复核

- 服务端 `py -3.12 -m pytest -q` 通过 14/14；Android 的 `clean test lint assembleDebug` 和随后单独执行的 `connectedDebugAndroidTest` 均通过，仪器测试 32/32。
- 复核了服务端 Compose 配置，`docker compose config --quiet` 通过；镜像构建因本机 Docker Desktop Linux 引擎未运行而无法完成，代码和配置未因此修改。
- 阶段 11～13 已完成，阶段 15 的文字记录同步继续保持可用；COS、云服务器部署、Agent、两台真实设备、API 24、弱网和 50+ 记录压力验收仍留待后续。

## 2026-09-16：阶段 15 文字记录接入（部分验收）

- Android 增加 Retrofit/Gson API、登录/Token 刷新及个人中心共享账号入口；Debug 模拟器默认连接 `10.0.2.2:8000`，正式地址由构建参数指定。
- 保留既有应用备份设置，仅从云备份和设备迁移中排除包含 Token 的 `cloud_session.xml`。
- Room 升至版本 3，新增可空 `serverId` 和 2→3 迁移；远端刷新只更新云端映射行，不删除旧本地旅行与照片。旧本地旅行存在时会阻止登录，避免混合数据。云端新增、编辑、删除文字记录先写服务端，再刷新本地缓存；云端照片明确提示暂未开放。
- Android 完整 `clean test lint assembleDebug connectedDebugAndroidTest` 通过；后续单项变更重跑 API 34 `Pixel_6` 仪器测试，最终 32/32 通过。Room 1→2→3、旧数据保留、缓存合并、云端写入、登录控件、权限均覆盖。服务端 pytest 14/14 通过，并用独立 SQLite 真正启动 Uvicorn 完成健康检查、登录与 CRUD。
- 实际模拟器联调：个人中心登录后记录页显示远端旅行；另一 HTTP 会话编辑后在 App 刷新可见；App 删除最后一条记录后服务端旅行数变为 0。尚未做两台真实设备、API 24、弱网和 50+ 记录压力验收，也未接入 COS、云服务器或 Agent。

## 2026-09-16：阶段 13 共享账号认证

- 单一共享账号由本地命令初始化，不提供注册接口；初始密码和 Token Secret 由环境变量注入，数据库只保存 PBKDF2-SHA256 加盐哈希。
- 增加登录、刷新和当前用户接口；所有旅行、子记录、统计接口要求 Access Token，过期、类型错误和无效 Token 返回 401。
- 先写认证测试并确认因模块缺失失败；阶段 11～13 全量 pytest 12/12 通过。COS 图片与 Agent 端点尚未实现。

## 2026-09-16：阶段 12 旅行记录服务端 CRUD

- 实现 Trip 和 Record 的创建、列表/详情、局部编辑、删除接口，以及去重城市数、旅行次数、人民币总花费统计。
- 服务器校验旅行日期顺序、子记录日期上下界、类型、名称、评分、花费和备注；删除最后一条子记录会一并清理空旅行。
- 先写接口测试并确认首次因路由缺失返回 404；实现后阶段 11～12 全量回归 8/8 通过。阶段 13 将替换临时的单一用户读取依赖并保护所有业务接口。

## 2026-09-16：阶段 11 服务端基础架构

- 在 `feat/server-local` 建立 Python 3.12 / FastAPI 项目，SQLAlchemy 2 的 `User → Trip → Record → RecordImage` 数据关系及 SQLite 外键，Alembic 初始迁移。
- 增加统一环境配置、404 错误格式、`GET /api/v1/health`、Dockerfile 与本地 Compose 数据卷。当前仅提供基础服务和图片元数据表，不提供图片上传。
- 测试先行：初次 pytest 因服务端包不存在失败；实现后 4/4 pytest 通过，Compose 配置校验通过。无 Android 页面变化。
- 用户已有的客户端/服务端开发计划拆分保留；COS、云服务器、Agent 均不在本轮实现。

## 2026-09-15：阶段 10 发布前质量检查（API 34 部分完成）

- 清理 Demo 不需要的网络、相册读写、媒体读取和相机权限，并移除未使用的 FileProvider 配置。
- 新增清单仪器测试，确保最终合并清单不申请 Android 系统权限；AndroidX 自动生成的应用内签名权限不属于运行时权限。
- 执行 `clean test lint assembleDebug`，4 个 JVM 测试、Debug/Release 测试、lint 和 Debug APK 构建通过。
- API 34 `Pixel_6` 模拟器执行 26 个仪器测试，全部通过。
- 清空应用数据后冷启动成功，`am start -W` 状态为 `ok`，应用进程持续存在；最近日志中无崩溃、ANR、Room/SQLite 或权限异常。
- 尝试安装 API 24 平台和模拟器镜像，但 Google 下载连接被拒绝；API 24 设备回归以及 50 条以上记录滚动压力测试保留为阶段 10 未完成项。

## 2026-09-15：阶段 9 个人中心

- 将个人中心占位页替换为本地用户资料卡、旅行统计和关于信息。
- 统计展示去过的城市数、出行次数和人民币总花费，直接订阅已有 Room `TravelStats` Flow，数据变化后自动刷新。
- 版本信息从构建配置读取，当前显示 1.0.0；为此启用 AGP `BuildConfig` 生成。
- 新增 Compose 仪器测试覆盖非空统计和版本展示；模拟器验证空数据库显示 0 城市、0 出行和 ¥0.00。
- 阶段全量回归：4 个 JVM 测试、API 34 `Pixel_6` 模拟器 25 个仪器测试，以及完整 Debug/Release 构建和 lint。

## 2026-09-15：阶段 8 智能规划对话框架

- 将智能规划占位页替换为聊天式框架，包含标题栏、助手欢迎消息、消息展示区、多行输入框和发送按钮。
- 用户草稿使用 Compose 可保存状态，在页面状态恢复后继续显示。
- 点击发送显示“暂未开放，待完善”，不清空草稿，也不发起任何网络请求。
- 新增 2 个 Compose 仪器测试，覆盖输入后发送提示、草稿保留和保存状态恢复。
- 阶段回归：4 个 JVM 测试、API 34 `Pixel_6` 模拟器 24 个仪器测试及完整 Debug/Release 构建与 lint；模拟器手工检查界面和提示通过。

## 2026-09-15：阶段 7 旅行时间线、统计与数据库迁移

- 记录页改为 Trip 卡片时间线，按旅行开始日期倒序展示，每张卡片内按日期展示景点和美食子记录。
- 页面顶部实时显示去过的城市数和出行次数；数据层同时提供总花费统计供个人中心复用。
- 主 `+` 用于创建旅行并填写城市、起止日期和第一条子记录；卡片内 `+` 用于向已有旅行继续添加记录。
- 新增旅行和子记录日期范围校验，编辑页展示所属旅行时间；多日旅行禁止把子记录日期改到范围外。
- 删除最后一条子记录时同步删除空 Trip。
- 实现 Room 1 → 2 migration，将旧版每条记录迁移为同城市、同日期的单日 Trip，并保留记录字段和主键。
- 自动化回归覆盖 4 个 JVM 测试及 API 34 `Pixel_6` 模拟器 22 个仪器测试；完整 `build` 包含 Debug/Release 与 lint。
- 模拟器手工验证：新建北京旅行后统计为 1 城市/1 出行；追加第二条记录后卡片显示 2 条；详情与编辑页可进入；依次删除后记录数变为 1，删除最后一条后恢复空状态。

## 2026-09-15：阶段 6 完整行政区划

- 内置国家统计局 2023 年公开数据快照，包含 31 个省级、342 个地级和 3056 个县级条目，共 3429 条。
- 新增行政区划 Repository，启动时不全量写入 Room；资源首次按需解析并在内存缓存，只保存用户实际选择的地区。
- 地区选择器支持省份横向筛选、城市/自治州/区县中文搜索、完整层级路径和直辖市显示处理。
- 省级与地级代码统一规范为 6 位；同名区县按行政区划代码区分，已有相同代码的旧预置城市会更新为规范名称和代码。
- 移除不再使用的 20 城市启动初始化协程；数据来源、转换规则和 MIT 许可证记录在 `城市数据源.md` 及 App assets 中。
- `testDebugUnitTest connectedDebugAndroidTest` 回归通过：2 个 JVM 测试、API 34 `Pixel_6` 模拟器 16 个仪器测试。模拟器实际打开选择器并成功回填“北京市”。

## 2026-09-15：阶段 5 照片本地化

- 创建和编辑记录时，将 Photo Picker 返回的图片压缩为最长边 1080px、JPEG 质量 80，并保存到 App 内部 `files/photos` 目录。
- 数据库仅保存内部照片路径，避免依赖相册临时 URI；已有内部照片在编辑时直接复用。
- 编辑替换或移除照片后清理旧副本，删除记录后清理该记录的全部照片；删除操作只允许作用于 App 管理的照片目录。
- 数据库写入失败时回滚本次新建的照片文件，避免产生孤立文件。
- `testDebugUnitTest connectedDebugAndroidTest` 回归通过：2 个 JVM 测试、API 34 `Pixel_6` 模拟器 12 个仪器测试，其中新增压缩/存储/删除、编辑替换清理和外部文件删除保护用例。

## 2026-09-15：阶段 2–4 本地页面与 CRUD 补全

- 记录列表改用 `records → trips → cities` 联表查询，卡片显示真实城市名称。
- 新增统一 `RecordInputValidator`，校验必选城市、必填名称、名称最多 100 字、备注最多 2000 字、花费 0～10000000 元和照片最多 9 张。
- 新增与编辑页面共用校验规则；达到 9 张照片后隐藏继续添加入口，错误信息保持显示。
- `testDebugUnitTest` 通过 2 个校验器用例；API 34 模拟器 `connectedDebugAndroidTest` 通过 9 个 Room 用例。

## 2026-09-15：阶段 1 本地数据层（数据库迁移除外）

### 实现范围

- 新增 `TripEntity`、`TripDao` 和 `TripRepository`，Room 数据库升级为版本 2。
- 数据关系调整为 `City → Trip → Record`；`RecordEntity` 改为通过 `tripId` 关联旅行。
- Trip 保存开始和结束日期，Repository 校验结束日期不得早于开始日期，子记录日期必须位于旅行时间段内。
- 新增记录时以所选日期创建单日 Trip，保持现有添加页面可用；编辑时在 Room 事务中同步更新 Trip 和 Record，并保留记录创建时间。
- 城市统计与按城市查询改为通过 Trip 联表；删除城市会由数据库级联删除 Trip 和 Record。
- Hilt 增加 `TripDao` 注入，详情页与编辑页改为通过 Trip 查询城市。
- 本阶段按当时要求暂不实现 Room 1 → 2 数据库迁移；该项后来在阶段 7 补齐。

### 验证结果

- TDD RED：`compileDebugAndroidTestKotlin` 因 Trip 类型和接口不存在而按预期失败。
- `assembleDebug assembleDebugAndroidTest`：通过。
- `build`：通过，Debug/Release 构建及 lint 完成，共 99 个任务。
- API 34 `Pixel_6` 模拟器，直接运行 `TripDataLayerTest`：`OK (8 tests)`。
- 覆盖 Trip DAO/Repository CRUD、Record Repository CRUD、按城市查询、日期范围、单日 Trip 创建、事务更新和城市级联删除。
- `connectedDebugAndroidTest`：通过，在 API 34 `Pixel_6` 模拟器执行 8 个测试。
- 首次运行时 UTP 因固定临时目录 `G:\tmp` 不存在而报 `AccessDeniedException`；创建该目录后标准 Gradle 测试命令恢复正常。
- 最终 Debug APK 覆盖安装成功，冷启动状态为 `ok`，应用进程持续存在，启动日志无 `FATAL EXCEPTION` 或 Room/SQLite 异常。

### 当前可见效果

- 原有新增、详情、编辑和删除页面继续使用三层数据模型。
- 当前界面仍以单条记录为操作单位，每次新增会生成一条单日 Trip；Trip 时间段录入、旅行卡片和出行次数统计留到后续界面阶段。

## 2026-09-15：文档状态同步

- 根据当前源码重新核对 README、需求、开发计划、测试指南和 CRUD 里程碑总结。
- 将目标需求与当前 Demo 实现分开，明确 Trip、3000+ 城市、照片本地化、智能规划和个人中心仍待实现。
- 更新测试范围和 PowerShell/ADB 命令，删除已经失效的“时间线 / 足迹 / 添加”底部导航说明。
- 回归结果：`.\gradlew.bat build` 成功，99 个任务完成；Markdown 本地链接检查通过；`git diff --check` 通过。

## 2026-09-15：Footmarks Demo CRUD 里程碑

提交：`d8c668a Footmarks demo: complete CRUD records`

分支：`feat/demo`

远程：`origin/feat/demo`

### 本次实现

1. 项目基础
   - 建立 Kotlin、Jetpack Compose、Material 3 项目。
   - 配置 Room、Hilt、Navigation Compose、Coil 和 Java Time desugaring。
   - 支持 Android 7.0（API 24）至 Android 14（API 34）。

2. 数据层
   - 建立 `CityEntity` 和 `RecordEntity` 两张 Room 表。
   - 建立 DAO、Repository、类型转换器和 Hilt 数据库模块。
   - `RecordEntity.cityId` 关联城市；城市删除时数据库级联删除记录。

3. 城市选择
   - 首次启动写入北京、上海、广州等 20 个常用城市。
   - 支持按中文名称搜索和手动创建城市。
   - 解决了占位城市 ID 不存在时保存失败的问题。

4. 主界面
   - 底部导航调整为“记录 / 智能规划 / 个人中心”。
   - 添加记录入口改为记录页右下角 `+`。
   - 智能规划和个人中心保留为占位页。

5. 记录 CRUD
   - 添加景点或美食记录。
   - 按实际游览日期倒序显示记录列表。
   - 查看记录详情和真实城市名。
   - 编辑当前记录字段。
   - 删除记录前弹出确认对话框。

### 当前可见效果

- 启动后默认进入“记录”页。
- 空数据库显示空状态；点击 `+` 进入添加表单。
- 保存后列表自动响应 Room 数据变化。
- 点击卡片进入详情，详情页可编辑或删除。
- 三个底部 Tab 可以切换。

### 验证记录

- Gradle `assembleDebug`：通过。
- Gradle `build`：2026-09-15 回归通过，共执行 99 个任务；项目没有自定义测试源码，测试任务显示 `NO-SOURCE`。
- APK：`app/build/outputs/apk/debug/app-debug.apk`。
- 真机/模拟器 CRUD 全流程：尚未形成可复核的完整测试记录，不能视为最终验收。

### 已知差距

- 当前数据模型为 `City → Record`，需求中的 `Trip` 时间段尚未实现。
- 目前只有 20 个常用城市，不是约定的 3000+ 行政区划数据。
- `PhotoManager` 已有压缩和内部存储代码，但 `RecordRepository` 尚未调用它；当前只保存相册 URI。
- 删除记录不会删除照片文件；照片数量没有 9 张上限。
- 记录列表的城市名称仍是占位文字，详情页才显示真实城市。
- 智能规划没有输入框和发送提示，个人中心没有统计内容。
- 尚无项目自定义单元测试或 UI 自动化测试。

## 2026-09-19：Agent Phase 12 异常处理和边界测试

### 实现范围和效果

- 新增 `server/tests/test_agent_phase12.py`，覆盖空历史、工具空结果、工具异常、核心天气信息失败、开放时间缺失以及 ReAct/Validator 轮次上限。
- 空历史查询现在表示“查询成功但没有历史记录”，标准化为 `TravelHistoryInfo(trip_count=0)`，不会重复查询。
- 核心天气信息连续失败后，Graph 会在有限重试后进入最终回答，并明确说明天气不可用和失败原因。
- 修正 `ReActCollector` 的空结果判断顺序，避免把成功的空历史结果错误降级为 `unavailable`。

### 验证结果

- Phase 12 相关 Agent 回归：`63 passed`。
- `python -m compileall -q server/app server/tests` 通过。
- `git diff --check` 通过。
- pytest 使用服务器上的一次性测试容器执行，未修改生产容器或服务配置。

### 后续处理

- Phase 13 继续建立真实场景端到端测试；真实 LLM、HTTP Agent API 和真实数据源暂未接入。

## 2026-09-19：固定 Agent 开发测试容器

- 新增 `server/Dockerfile.dev` 和 `server/compose.dev.yaml`，固定 Python 3.12、项目依赖和 pytest 环境。
- 新增长期运行的 `agent-test` 容器，源码通过目录挂载；代码修改后直接执行容器内 pytest，不再为每次回归临时安装依赖或创建一次性容器。
- 生产 `server/Dockerfile` 和 API Compose 配置保持独立，线上镜像不额外安装测试依赖。
- README 增加首次构建和日常测试说明。

## 2026-09-19：Agent Phase 13 端到端场景

### 实现范围和效果

- 新增 `server/tests/test_agent_phase13.py`，覆盖天气、历史、POI、预算、路线、三日行程、历史去重、复杂需求、开放时间未知和路线时间冲突共 10 个场景。
- Graph 增加 `react_action` 状态，支持行程规划在 POI 完成后继续动态发现路线、预算等信息，同时避免天气等普通请求重复调用已完成 Tool。
- 路线时间不足场景完整经过 Validator → Local Reviser → Validator，最终得到修订后的有效行程。
- 修正 `server/Dockerfile.dev` 对基础依赖文件的复制，固定测试容器可以正常构建。
- 统一 Agent 模块命名：`information_status.py` 改为 `information.py`，`tools/internal_db.py` 改为 `tools/internal.py`，同步更新测试文件名和导入。

### 验证结果

- Phase 13 场景：`10 passed`。
- Agent 非数据库 fixture 回归：`141 passed`；Internal DB Agent 回归：`5 passed`；合计 `146 passed`。
- 固定测试容器运行于服务器 `/home/ubuntu/footmarks-agent-test`，没有修改生产容器、生产 `.env` 或生产数据库。

### 后续处理

- Phase 14 增加结构化日志和可观测性；真实 LLM、HTTP Agent API 和真实高德网络数据仍待接入。

## 2026-09-19：Agent Phase 14 日志与可观测性

### 实现范围和效果

- 新增 `server/app/agent/observability.py`，统一 Agent 结构化事件模型和观察器接口。
- 为一次 Agent 运行生成 `request_id`，并记录可选 `user_id`、节点、Tool、信息状态、ReAct/Validator 轮次和耗时。
- Collector、Graph、Workflow 已接入事件记录；Tool 生命周期、行程生成、校验和最终响应都可以被关联查看。
- 日志只保留必要的状态和错误码，不记录 Prompt、Tool 参数、原始响应或 LLM 隐藏推理。
- 当前效果面向内部调试，暂未接入 SSE、Agent HTTP API 或生产日志平台。

### 验证结果

- 可观测性专项测试：`4 passed`。
- Agent 非数据库回归：`145 passed`；Internal DB Agent 回归：`5 passed`；Phase 1～14 合计 `150 passed`。
- 固定测试容器运行于服务器 `/home/ubuntu/footmarks-agent-test`；生产容器、生产 `.env` 和生产数据库未修改。
- Python 模块编译检查、Compose 配置检查和 `git diff --check` 通过。

### 后续处理

- 进入 Phase 15 最终代码检查；真实 LLM、HTTP Agent API、SSE 事件流和真实高德网络数据仍待后续接入。

## 2026-09-19：Agent Phase 15 最终代码检查

### 检查范围和结果

- 按 `Agent架构.md` 检查 Workflow/ReAct 分工、结构化 State、Tool 与 Graph 解耦、Validator 与 LLM 解耦，以及没有独立 Information Planner。
- 验证信息尝试次数、ReAct 轮次、Validator 轮次、`unavailable`/`failed` 降级、UNKNOWN 不触发修订和 FAIL 触发修订。
- 验证开放时间、天气、路线缺失不会被虚构，预算会明确作为估算，Final Response 只使用 Structured State 中已有事实。
- 验证用户看不到未验证行程、Tool 原始响应、ReAct 决策和 Validator 中间结果。

### 验证结果

- 固定测试容器补齐 `alembic.ini` 和 `alembic/` 迁移目录后，服务端全量测试 `169 passed`。
- Agent Phase 1～15 相关回归 `150 passed`；`pip check` 无损坏依赖。
- Python `compileall`、开发/服务端 Compose 配置检查和 `git diff --check` 通过。
- 本次只修复了固定测试容器的文件同步范围，生产容器、生产 `.env` 和生产数据库未修改。

### 当前边界

- Agent V1 计划中的 Phase 0～15 已完成。真实 LLM、HTTP Agent API、SSE 事件流和真实高德网络端到端测试作为后续接入工作。

## 2026-09-19：接入 API Task 1：LLM 服务端配置

### 实现范围

- 为服务端 Settings 增加 LLM Base URL、API Key、模型名、请求超时和最大重试次数配置。
- 更新 `.env.example` 和 Compose 环境变量映射；真实 `server/.env` 未修改。
- 将 `httpx` 纳入生产依赖，开发依赖不再重复声明。
- 新增配置加载、默认未配置状态和超时/重试边界测试。

### 验证结果

- Task 1 定向测试：`4 passed`。
- 固定测试容器服务端全量回归：`173 passed`；`pip check` 无损坏依赖。
- Python 编译检查、开发/服务端 Compose 配置检查和 `git diff --check` 通过。

### 后续处理

- 下一步执行接入 API Task 2：实现结构化 LLM 客户端；当前尚未调用真实 LLM。

## 2026-09-19：接入 API Task 2：结构化 LLM 客户端

### 实现范围

- 新增 `server/app/agent/llm.py`，实现兼容 OpenAI Chat Completions 的同步 JSON Schema 请求。
- 统一处理未配置、超时、网络失败、429/5xx 重试、非 2xx 响应、非法 JSON 和 Pydantic 校验失败。
- 结构化客户端返回指定 Pydantic 模型，异常信息不包含 API Key、Prompt 或原始模型响应。
- 使用 `httpx.MockTransport` 覆盖请求体、鉴权头、重试和异常边界；未调用真实 LLM。

### 验证结果

- Task 2 客户端专项测试：`8 passed`。
- Analyzer、Generator、Reviser 兼容回归：`33 passed`。
- 固定测试容器服务端全量回归：`181 passed`；`pip check` 无损坏依赖。
- Python 编译检查和 `git diff --check` 通过。

### 后续处理

- 下一步执行接入 API Task 3：用结构化 LLM 输出驱动 ReAct Tool 决策。

## 2026-09-19：接入 API Task 3：ReAct LLM 决策

### 实现范围

- 将 `TOOL_INFORMATION_NEEDS` 提升为公共常量，供后续运行时过滤可用 Tool。
- 新增 `LLMReActDecisionClient`，将 `ReActContext` 序列化后交给结构化 LLM 客户端，并返回 `ReActDecision`。
- 增加决策提示词约束：每轮最多一个 Tool Call、只能选择可用 Tool、`reason` 仅保留简短操作说明，不输出思维链。
- 对 LLM 返回的未知 Tool 名称进行白名单校验，在执行 Tool 前抛出 `LLMInvalidResponseError`。
- 增加合法 Tool Call、无 Tool 停止、未知 Tool 和思维链约束测试；未调用真实 LLM。

### 验证结果

- Task 3 决策专项、Collector 和信息状态回归：`22 passed`。
- 固定测试容器服务端全量回归：`185 passed`；`pip check` 无损坏依赖。
- Python 编译检查和 `git diff --check` 通过。
- 仍存在项目原有的 Starlette `BlockingPortal` 弃用警告，不影响测试结果。

### 关联提交与后续处理

- 关联提交：`feat: connect LLM ReAct decisions`。
- 下一步执行接入 API Task 4：组装真实 Agent Runtime；当前仍未接入 HTTP Agent API。

## 2026-09-19：接入 API Task 4：组装 Agent Runtime

### 实现范围

- 新增 `AgentRuntime` 和 `AgentRunResult`，按请求创建并执行完整 LangGraph。
- 内部数据库 Tool 按传入的 `user_id` 创建，确保 Agent 只能读取当前用户的旅行记录。
- 合并预算 Tool 和高德 Web Tool，但只向 ReAct 暴露已有结构化信息映射中的工具；`geocode`、`reverse_geocode` 不会进入可用工具描述。
- 将同一个结构化 LLM 客户端注入需求分析、ReAct 决策、行程生成和行程修订模块；观察器事件保留请求 ID 和用户 ID。
- 通过依赖注入支持 Fake LLM、Fake 高德传输和测试观察器，测试不访问真实模型或外部网络。

### 验证结果

- Runtime 专项测试：`7 passed`。
- Runtime、Graph、Phase13 回归：`22 passed`。
- 固定测试容器服务端全量回归：`192 passed`；`pip check` 无损坏依赖。
- Python 编译检查和 `git diff --check` 通过。
- 仍存在项目原有的 Starlette `BlockingPortal` 弃用警告，不影响测试结果。

### 关联提交与后续处理

- 关联提交：`0e8c591 Agent API: assemble production runtime`。
- 下一步执行接入 API Task 5：提供受认证的 `POST /api/v1/agent/chat`；当前尚未创建 HTTP Agent 路由。

## 2026-09-19：接入 API Task 5：受认证的 HTTP Agent API

### 实现范围

- 新增 `POST /api/v1/agent/chat`，复用现有 Bearer Token 认证和数据库会话。
- 增加消息长度与空白校验：去除首尾空白后，消息长度限制为 1～2000 个字符。
- 通过 `create_app(agent_runtime=...)` 支持注入 Fake Runtime；生产环境默认使用真实 `AgentRuntime`。
- 将 LLM 未配置、超时、上游失败和结构化响应失败分别映射为 `503`、`504`、`502`、`502`，不暴露内部响应内容。
- API 成功响应严格只包含 `request_id` 和 `answer`。

### 验证结果

- API 契约专项测试：`7 passed`。
- API 和认证回归：`11 passed`。
- 固定测试容器服务端全量回归：`199 passed`；`pip check` 无损坏依赖。
- Python 编译检查和 `git diff --check` 通过。
- 仍存在项目原有的 Starlette `BlockingPortal` 弃用警告，不影响测试结果。

### 配置与关联提交

- Task 5 没有新增 API Key。真实 Agent 调用仍要求服务器 `.env` 已配置 `FOOTMARKS_LLM_BASE_URL`、`FOOTMARKS_LLM_API_KEY` 和 `FOOTMARKS_LLM_MODEL`；未配置时接口会返回 `503`。
- 关联提交：`c79fbc3 Agent API: expose authenticated chat endpoint`。
- 下一步执行接入 API Task 6：增加 LLM/API 可靠性和脱敏回归。

## 2026-09-19：接入 API Task 6：LLM/API 可靠性和脱敏回归

### 实现范围

- 增加 429 重试上限测试，确认实际请求次数不超过 `llm_max_retries + 1`。
- 增加 LLM 超时不会进入无限 ReAct 循环的测试，确认超时在第一次决策时直接透传。
- 增加非法行程边界测试；发现并修复运行时 `ValueError` 未映射为 HTTP 响应的问题，现在返回脱敏的 `502`。
- 增加日志和 HTTP 响应脱敏回归，确保 API Key、Prompt、Tool 参数、原始响应以及 `information_status`、`collected_info`、`itinerary`、`validation`、`reason` 不会泄露。

### 验证结果

- Task 6 边界测试：`10 passed`。
- Agent 全量测试：`184 passed`。
- 固定测试容器服务端全量回归：`203 passed`；`pip check` 无损坏依赖。
- Python 编译检查和 `git diff --check` 通过。
- 仍存在项目原有的 Starlette `BlockingPortal` 弃用警告，不影响测试结果。

### 配置与关联提交

- Task 6 没有新增 API Key 或环境变量。
- 关联提交：`1158dc3 Agent API: cover LLM failure boundaries`。
- 下一步执行接入 API Task 7：Android 接入 Agent HTTP 数据层。

## 2026-09-19：接入 API Task 7：Android Agent HTTP 数据层

### 实现范围

- 新增 `AgentChatRequest` 和 `AgentChatResponse`，映射服务端 `message`、`request_id` 和 `answer`。
- 为 `FootmarksApi` 增加认证的 `POST /api/v1/agent/chat` 方法。
- 为 `CloudSession` 增加 `askAgent(message)`，复用现有 `authorized` 逻辑处理 401、刷新 Token 和单次重试。
- 增加 MockWebServer 契约测试，覆盖请求方法、路径、Authorization、请求 JSON、响应解析和刷新后的新 Token。

### 验证结果

- 首次测试按预期因接口和数据类型不存在而失败。
- 强制重跑 KSP 后，Agent API 定向单元测试构建通过。
- 标准 `testDebugUnitTest --tests "*FootmarksApiTest"` 通过。
- Android `testDebugUnitTest` 全量通过。
- 本次未修改本地数据库实体；首次构建发现的过期 Room/KSP 生成代码已通过 `clean` 和 `--rerun-tasks` 重新生成解决。
- 构建仍提示本机 Android SDK XML 版本兼容性警告和既有未使用参数警告，不影响测试结果。

### 配置与关联提交

- Task 7 不需要新增 API Key 或环境变量，继续使用现有 Retrofit Base URL 和登录 Token。
- 关联提交：`118cff3 Agent API: add Android chat client`。
- 下一步执行接入 API Task 8：Android 智能规划页面接入 ViewModel。

## 2026-09-19：接入 API Task 8：Android 智能规划页面

### 实现范围

- 新增 `SmartPlanningViewModel`、`SmartPlanningUiState`、`ChatMessage` 和 `ChatRole`。
- 接入 `CloudSession.askAgent()`：用户消息立即进入列表，成功后追加 Agent 回复并清空草稿。
- 发送期间禁用重复提交；空输入不发送；未登录、401 和 502/503/504 显示可理解的错误并保留草稿及历史消息。
- 使用 `SavedStateHandle` 保存草稿，支持页面状态恢复。
- 替换原有“暂未开放，待完善”页面，增加可注入的 `SmartPlanningContent`，并在消息变化后滚动到最新内容。
- 增加 ViewModel 协程测试和 Compose 仪器测试，均不访问真实网络。

### 验证结果

- Android `testDebugUnitTest` 全量通过。
- Pixel_6 API 34 仪器测试：`34 tests` 全部通过。
- 构建过程中只出现既有的 SDK XML 版本提示和未使用参数警告，不影响测试结果。
- `git diff --check` 通过。

### 配置与关联提交

- Task 8 不需要新增 API Key 或环境变量。
- 关联提交：`d096b63 Agent API: connect smart planning UI`。
- 下一步执行接入 API Task 9：真实 LLM 与服务器部署验收；该阶段需要你提供或确认 LLM 服务商配置。

## 2026-09-19：接入 API Task 9：服务器部署与真实 LLM 验收

### 已完成

- 检查本机 `server/.env` 的五项 LLM 配置，只将这五项合并到服务器现有 `server/.env`，没有覆盖服务器已有 Token、COS、高德或数据库配置。
- 将当前 `feat/demo` 已提交源码部署到服务器；服务器目录不是 Git 工作树，因此使用 `git archive` 传输已跟踪源码，不包含本机 `.env`、数据库和密钥。
- 执行 `docker compose --env-file server/.env -f server/compose.yaml up -d --build`，API 容器重建并运行；Alembic 迁移和 Uvicorn 启动日志正常。
- 公网 `https://www.cq-footmark.online/api/v1/health` 返回 HTTP 200。
- 本机执行 `assembleDebug -PfootmarksApiBaseUrl=https://www.cq-footmark.online/` 成功，APK 生成于 `app/build/outputs/apk/debug/app-debug.apk`。

### 真实验收结果

- 修正服务器 `FOOTMARKS_LLM_BASE_URL` 后，真实 HTTPS Agent 请求通过，响应包含非空 `request_id` 和回答。
- 天气、历史旅行、POI 推荐、预算估算、路线查询和三日行程六类场景均返回 HTTP 200；测试脚本只记录状态、请求 ID 是否存在和回答长度，不输出 Key、Token 或模型原文。
- 为提升真实模型稳定性，补充了结构化输出字段约束、Tool 参数别名归一化、非法结构重试和行程天数精确约束；生成器随后通过线上三日行程验证。
- 手机真机登录、智能规划交互、错误重试和连续发送检查仍需用户在自己的设备上完成。

## 2026-09-19：修复历史查询 Tool 归一化失败

### 根因

- 用户查询“我之前去过哪些地方”时，真实模型选择了 `get_travel_summary`。
- 该 Tool 返回 `TravelSummary` Pydantic 模型，而历史归一化器只接受列表或字典，连续重试后产生 `Tool response could not be normalized`。

### 修改与验证

- 历史归一化器支持 Pydantic Tool 返回模型；统计模型至少可以安全转换为旅行次数，避免错误状态。
- 明确三个内部历史 Tool 的用途，并在 ReAct 提示中要求：地点/景点/美食查询使用 `search_trip_history` 或 `search_records`，统计查询才使用 `get_travel_summary`。
- 服务端全量回归从 208 项增加到 `211 passed`。
- 生产容器内运行时事件显示 `search_trip_history` 成功；公网 HTTPS 重放同一句查询返回 HTTP 200，包含 `request_id`。

### 验证结果

- 服务器健康检查：通过。
- API 容器状态和启动日志：通过。
- 真实 Agent HTTPS 请求：通过，六类场景 6/6 返回 HTTP 200。
- Android Debug APK 构建：通过。
- 服务端固定测试容器：`208 passed`，1 条第三方弃用警告。
- 关联代码提交：`054238b`、`3eddcd4`；文档提交待本地完成。

## 2026-09-19：修复 Android Agent 历史查询超时

### 根因

- 在生产服务器复现“我之前去过哪些地方”时，请求实际耗时约 16 秒并返回 HTTP 200。
- Android Retrofit 使用 OkHttp 默认约 10 秒的读取/调用超时，客户端先于服务端完成响应而显示 timeout；数据库历史工具和服务端 Agent 流程本身没有失败。

### 修改与验证

- `FootmarksApi` 的 OkHttp 客户端将读取和总调用超时设置为 120 秒，连接和写入超时设置为 20 秒。
- 新增长响应超时回归测试，验证客户端配置；完整 Android JVM 测试通过。
- API 34 `Pixel_6` 模拟器 34 项仪器测试通过，HTTPS Debug APK 构建通过。

### 后续

- 用户重新安装最新 APK 后，再测试历史查询、天气查询和三日行程；手机真机验收仍由用户完成。

## 2026-09-19：修复智能规划发送后的输入框状态

### 修改

- 点击发送后立即清空输入框，避免请求等待期间重复看到已经发送的文本。
- 如果 Agent 请求失败，恢复原草稿并保留用户消息，方便点击发送重新尝试；请求成功后输入框保持为空。

### 验证

- 新增“请求进行中输入框为空”的 ViewModel 测试。
- Android JVM 测试、API 34 `Pixel_6` 模拟器 34 项仪器测试和 HTTPS Debug APK 构建全部通过。

## 2026-09-20：智能规划错误提示与消息复制

### 修改

- 线上 HTTPS Agent 接口只读回归返回 HTTP 200；此前 Android 的“智能规划服务暂不可用”同时覆盖 502、503、504，无法判断具体原因。
- Android 按状态码区分上游调用失败、LLM 配置缺失和响应超时，方便定位服务器 `.env` 或上游服务问题。
- 智能规划用户消息和 Agent 回复加入长按选择复制，保留原有失败恢复草稿功能。

### 验证

- 线上登录、历史查询、景点推荐请求：HTTP 200。
- `testDebugUnitTest`、`assembleDebugAndroidTest`、HTTPS Debug APK 构建：通过。
- 本机未连接 Android 设备，未执行连接设备仪器测试。

## 2026-09-23：Agent 稳定性计划 P0 请求级可观测性

### 实现范围

- FastAPI 请求入口生成或接收 `request_id`，通过 `X-Request-ID` 与 Nginx 对接，并在响应中返回同一个 ID。
- Nginx access log 增加请求 ID，反向代理将 `$request_id` 转发给 API。
- 使用请求上下文把 ID 传递到 Agent Runtime、LangGraph、ReAct Decision、LLM 日志和 Tool 事件。
- 为 Requirement Analyzer、ReAct Decision、Itinerary Generator、Validator、Reviser、Final Response 增加阶段开始、结束、耗时和状态事件；Tool 执行与归一化继续记录完成耗时和错误码。
- 日志只保留请求 ID、阶段、耗时、状态和错误类型，不记录 API Key、完整 Prompt、原始模型响应或隐藏推理。

### 验证结果

- 新增请求上下文恢复、Runtime 请求 ID 传播、API 日志关联、Nginx ID 回传和 LLM 日志关联测试。
- Agent 相关测试：`204 passed`，1 条既有 Starlette `BlockingPortal` 弃用警告。
- 服务端全量回归：`228 passed`，1 条既有 Starlette `BlockingPortal` 弃用警告；从仓库根目录执行以隔离本机 `server/.env` 中的真实 COS 凭据。
- P0 代码完成后暂停，服务器重建、Nginx 配置检查和手机真实请求验收留待下一步手工执行。

## 2026-09-23：Agent 稳定性 P0 部署到服务器

### 部署内容

- 使用项目专用部署密钥连接 `43.135.114.5`，部署提交 `fc2d932` 的服务端代码。
- 本地部署包与服务器临时包 SHA-256 一致；部署包不包含服务器 `.env` 和数据库。
- 覆盖代码前备份 `/opt/footmarks/server`，服务器 `.env` 与 `data/footmarks.db` 保留。
- Nginx 先备份 `/etc/nginx/sites-enabled/footmarks`，加入 `$request_id` 日志、`X-Request-ID` 转发和 API access log；保留现有 `client_max_body_size 50m` 与 `proxy_read_timeout 300s`。
- Nginx 配置检查通过并热重载；API 容器使用服务器现有 `.env` 重建启动，Alembic 和 Uvicorn 启动正常。

### 验证结果

- `server-api-1` 状态为 running。
- `https://www.cq-footmark.online/api/v1/health` 返回 HTTP 200。
- Nginx access log 已出现 `request_id` 字段。
- 待用户在手机发送一条已登录 Agent 消息，继续验证 API、LLM、Tool 日志中的同一请求 ID和最终状态码。

## 2026-09-23：修复 Agent 业务日志不可见

### 根因与修改

- Nginx access log 已记录 `request_id`，但 API 容器此前只有 Uvicorn 访问日志，`Agent request`、LLM 和结构化阶段事件的 INFO 日志没有输出。
- FastAPI 启动时显式配置 INFO 级别和应用日志 logger，保留原有脱敏规则。
- 新增日志级别回归测试，避免后续部署再次只看到 HTTP 访问日志。

### 验证结果

- 日志与 Agent 定向回归：`20 passed`。
- 服务端全量回归：`229 passed`，1 条既有 Starlette `BlockingPortal` 弃用警告。
- 提交 `a0a1627` 已部署，容器重建并运行，公网健康接口返回 HTTP 200。
- 下一条已登录 Agent 请求需要再次检查 API 容器业务日志中的同一 `request_id`。

## 2026-09-23：P0 手机实测与规划链路优先级调整

### 验证结果

- 手机历史查询请求完成了 401、Token 刷新和最终 200 重试；Nginx access log 与 API 业务日志的最终 `request_id` 一致。
- 规划请求一条耗时约 145 秒后返回 200；另一条耗时约 208 秒，客户端提前断开并产生 Nginx 499，但服务端仍继续执行到结束。
- 规划链路中 ReAct LLM 单次调用达到约 52 秒和 67 秒，Tool 与 Validator 只有毫秒级耗时；问题核心是 LLM 调用次数、重复失败 Tool 和客户端断开后的继续执行。

### 计划调整

- P1 先验收历史、天气、景点、预算、路线五类基础问答。
- P2 按“限制 ReAct 轮数、停止重复失败 Tool、增加阶段预算、处理客户端断开、统一超时映射”的顺序处理规划稳定性。
- P3 再处理一日游、多日游和必要的异步任务/流式进度；P4 保留给多轮上下文和其他扩展能力。

## 2026-09-23：修复历史范围查询和路线地点解析

### 根因与修改

- search_trip_history 之前只返回旅行城市和时间段，没有加载旅行下的景点/美食记录；现在会返回记录明细，并支持城市与 ATTRACTION/FOOD 类型筛选。
- ReAct Tool 参数缺失时，Collector 现在会从结构化需求补充历史查询城市、记录类型、路线起点终点和距离起点终点。
- 历史回答现在区分“去过哪些城市”和“某城市去过哪些景点/美食”；没有匹配城市时明确返回没有历史记录。
- 高德路线和距离 Tool 现在会把中文地点名先地理编码为经纬度，再调用路线/距离接口；原本已经是经纬度的参数保持直接调用。
- 需求分析新增 history_category，用于保留用户询问的是景点还是美食。

### 验证结果

- 新增历史记录明细、城市/类型筛选、Tool 参数补全、路线地理编码和无匹配城市回答测试。
- 定向回归：39 passed；SQLite 历史 Tool：8 passed。
- 服务端全量回归：236 passed，1 条既有 Starlette BlockingPortal 弃用警告。
- 服务器真实路线和手机历史分类查询仍需部署后手工验收。

## 2026-09-23：Agent 稳定性 P1 基础问答验收完成

### 实现与修复

- 历史查询会从“去过哈尔滨哪些地方”中提取 `destination=哈尔滨`，并按城市过滤历史记录；没有记录时返回明确的无记录提示。
- 路线查询在未提供城市时，会优先匹配两个 POI 的共同行政区，避免中山陵和夫子庙被解析到不同城市。
- 高德路线接口返回 `ENGINE_RESPONSE_DATA_ERROR` 时，服务端会使用 POI 坐标自动重试，减少额外的 LLM 轮次。

### 验证结果

- P1 五类基础问答已在真实手机上完成：历史、天气、景点、预算和路线。
- 需求分析、历史响应、高德路线和 Agent Collector 定向回归通过；新增历史城市提取和同城路线 POI 匹配测试。
- 服务器部署版本：`80f87b6`；API 健康检查返回 `{"status":"ok","service":"footmarks-api"}`。
- P1 完成，下一阶段按计划进入 P2，处理行程规划链路的耗时、重复调用和客户端断开。

## 2026-09-23：Agent 稳定性 P2-1 限制 ReAct 轮数

### 实现内容

- 将 `ReActCollector` 默认最大轮数从 8 轮调整为 4 轮，控制同步规划请求的 LLM 调用次数。
- 保留达到轮数上限后的受控返回行为，不再继续发起新的 ReAct Decision 或 Tool 调用。
- 新增回归断言，固定默认轮数预算，避免后续修改无意中放大移动端请求耗时。

### 验证结果

- `server/tests/test_agent_collector.py`：11 passed。
- 测试中的 Starlette 弃用提示和 pytest 缓存目录权限提示不影响断言结果。
- 下一步执行 P2-2：验证并强化终态失败 Tool 不被 ReAct 重复请求。

## 2026-09-23：Agent 稳定性 P2-2 停止重复失败 Tool

### 实现内容

- ReAct 上下文不再向 LLM 暴露已经进入 `completed`、`unavailable` 或 `failed` 终态的信息 Tool。
- 未初始化的信息项仍保留对应 Tool，行程规划可以继续发现新的路线、天气或预算需求。
- 增加回归用例，验证历史 Tool 终态失败后，下一轮只保留仍处于 `pending` 的 POI Tool。

### 验证结果

- `server/tests/test_agent_collector.py`：12 passed。
- 终态失败原因继续保存在 `InformationStatus.reason`，最终回答可以据此生成降级提示。
- 下一步执行 P2-3：增加各 LLM 阶段的单次与整条请求预算。

## 2026-09-23：Agent 稳定性 P2-3 增加阶段预算

### 实现内容

- 新增 `AgentBudget`，统一记录一次同步 Agent 请求的总耗时，以及 Requirement Analyzer、ReAct Decision、Itinerary Generator 和 Reviser 的累计阶段耗时。
- 默认总预算为 120 秒，单个 LLM 阶段预算为 60 秒；每次结构化 LLM 请求使用当前剩余预算作为 HTTPX 读取超时，避免重试或后续阶段突破总预算。
- Runtime 将同一个预算对象传入 Analyzer、ReAct Collector、Itinerary Generator 和 Reviser；预算耗尽抛出可映射到 HTTP 504 的 `AgentTimeoutError`。
- Compose 和 `.env.example` 增加 `FOOTMARKS_AGENT_TOTAL_TIMEOUT_SECONDS`、`FOOTMARKS_AGENT_STAGE_TIMEOUT_SECONDS`，不改动已有 `.env` 内容。

### 验证结果

- 预算模块与 Analyzer 阶段测试：5 passed。
- LLM、配置、Analyzer、Collector、Generator、Reviser 定向回归：63 passed。
- LLM HTTPX 请求超时覆盖测试通过；既有 Starlette 弃用提示仍存在，不影响断言。
- API 边界测试新增预算超时映射用例；本机 pytest 的 `tmp_path` 目录权限问题仍需单独处理，未将该环境问题误判为代码失败。
- 下一步执行 P2-4：处理客户端断开后的协作式停止。

## 2026-09-23：Agent 稳定性 P2-4 处理客户端断开

### 实现内容

- Agent API 改为异步入口，在运行同步 Agent 的工作线程同时轮询 `Request.is_disconnected()`。
- 客户端断开后设置线程安全取消信号，预算在下一个 Analyzer、ReAct、Tool、Generator 或 Reviser 阶段边界抛出 `AgentClientDisconnected`，阻止后续 LLM/Tool 调用。
- 取消信号通过 ContextVar 传递给 `AgentRuntime`，不改变 Fake Runtime 和普通调用方的 `run(message, user_id, db)` 接口。
- 断开请求记录 `Agent client disconnected` 和取消耗时；若取消发生在 API 仍可返回的窗口，映射为 HTTP 499。

### 验证结果

- 取消信号、预算边界、LLM 超时、配置、Analyzer、Collector、Generator、Reviser 和断开监控定向回归：65 passed。
- 服务端 Python 源码编译检查通过。
- 由于本机 pytest 临时目录存在 Windows 权限问题，依赖 `tmp_path` 的完整 API TestClient 用例仍需在权限正常的环境补跑；不影响本次无临时目录的 65 项结果。
- 下一步执行 P2-5：统一各层超时和状态映射。

## 2026-09-23：Agent 稳定性 P2-5 统一超时和状态映射

### 实现内容

- AMap 超时现在返回明确的 Tool 错误码 `amap_timeout`，作为可降级数据源错误保留在日志和回答提示中，不会把可回答的请求误报成 502。
- API 继续区分：LLM 未配置为 503，LLM 上游/结构化错误为 502，LLM 单次超时或 Agent 总/阶段预算耗尽为 504，客户端取消记录为 499。
- Nginx 配置增加连接、发送和响应超时：`proxy_connect_timeout=10s`、`proxy_send_timeout=15s`、`proxy_read_timeout=150s`、`send_timeout=15s`；与 Agent 120 秒总预算和 Android 150 秒请求预算保持顺序。
- Android 将 OkHttp 的读取/整个调用预算调整为 150 秒，并对 HTTP 499、SocketTimeoutException 和普通网络中断显示稳定的可重试提示。

### 验证结果

- AMap Tool 回归：10 passed，包含超时错误码断言。
- Android `SmartPlanningViewModelTest` 与 `FootmarksApiTest`：31 tests completed，BUILD SUCCESSFUL。
- P2 相关服务端定向回归继续通过；完整 API TestClient 回归仍受本机 pytest 临时目录权限问题影响。
- P2 服务端提交 `0f7e72b` 已部署；服务器归档 SHA-256 与本机一致，Nginx 配置检查和重载成功，API 容器启动正常，本机与公网健康接口均返回 HTTP 200。
- P2 完成。还需用手机验证 504、网络超时、客户端断开和正常问答链路。

## 2026-09-23：P2 真机回归问题修复

### 根因与实现

- 天气需求分析原先明确把“中秋”等节日日期置空，ReAct 又看不到原始日期，天气归一化后固定取预报列表第一项，因此会错误展示当天。现在需求结构保留 `date_expression`，Analyzer 向 LLM 提供服务器当前日期；天气 Tool 对日期查询强制使用预报模式，并只选择与 `start_date` 一致的数据。日期无法解析或预报范围不覆盖时返回明确的不可用原因，不再回退到当天。
- 南京三日游在 POI 已完成后继续查询可选距离；距离 Tool 失败并耗尽 4 轮时，Graph 原先直接进入降级回答。现在达到轮次上限后区分关键信息与可选信息：关键信息全部完成时继续进入 Itinerary Generator，只有关键信息缺失才返回降级回答。
- Android 请求成功时原先无条件把 `draft` 清空，请求失败时无条件恢复旧消息，都会覆盖等待期间输入的新内容。现在响应只更新消息和发送状态；失败时仅在当前草稿为空时恢复旧消息。
- 外层 Activity 与智能规划、个人中心内层 Scaffold 重复消费状态栏和导航栏 Insets。内层页面改为使用外层提供的安全区域，键盘显示时外层不再额外保留底部导航栏安全间距，使输入框贴近输入法并减少两个页面标题上方留白。

### 自动化验证

- 新增节日日期上下文、目标日期天气选择、未解析日期不回退当天、可选信息未完成仍生成行程，以及请求期间新草稿保留的边界测试；所有新增测试均先复现失败再修复。
- Agent 核心回归：189 passed；Phase 12/13 集成回归：14 passed；针对本次修改的定向回归：45 passed。
- Android 全量 JVM 单元测试和 HTTPS Debug APK 构建成功。
- 服务端全量套件已有 207 项通过，依赖 `tmp_path` 的 50 项仍受本机 Windows pytest 临时目录 `WinError 5` 影响；没有出现业务断言失败。
- 顶部和键盘布局需要在真实手机上做最终视觉验收。
- 修复提交 `c219a98` 已推送到 `fix/agent` 并部署到 `/opt/footmarks/server`；部署归档本机与服务器 SHA-256 一致，API 容器重建成功，服务器本机与公网 HTTPS 健康接口均返回 HTTP 200。

## 2026-09-23：修正智能规划输入框的重复键盘间距

- 真机截图显示输入法弹出后，输入框与键盘之间仍空出接近一整块键盘高度。
- 根因是系统窗口已经按输入法执行 resize，输入栏又单独使用 `imePadding()` 再次增加完整 IME 底部间距。
- 移除输入栏的 `imePadding()`，并在 `MainActivity` 明确配置 `android:windowSoftInputMode="adjustResize"`，统一由窗口层完成一次键盘避让。
- Android 全量 JVM 单元测试和 HTTPS Debug APK 构建通过；输入框与不同厂商输入法之间的最终间距由真机截图验收。

## 后续日志规则

每完成一个 [开发计划.md](./开发计划.md) 阶段，追加一条日志，至少包含：

- 日期与关联提交
- 实现范围和用户可见效果
- 构建、自动化测试和设备回归结果
- 新发现的问题与后续处理

## 2026-09-17：服务器基础部署

- 关联代码提交：`5185ed2`。
- 使用项目专用 SSH 公钥连接 Ubuntu 24.04 VPS，确认 `ubuntu` 用户具备 sudo 权限。
- 安装 Docker 29.1.3 和 Docker Compose 2.40.3，设置 Docker 开机启动。
- 将已验证的 `server/` 上传到 `/opt/footmarks/server`，生成服务器端 Token Secret，启动 `server-api-1` 容器。
- Alembic 已迁移到 `20260916_01 (head)`，SQLite 数据持久化在宿主机 `data/`，容器设置为异常退出自动重启。
- 服务器本机访问 `/api/v1/health` 返回 `{"status":"ok","service":"footmarks-api"}`。
- 初始化默认 `shared` 共享账号并完成回归，初始化密码未写入服务器环境或本地项目文件。
- 当时效果：FastAPI 已在服务器持续运行，但基础部署阶段只监听 `127.0.0.1:8000`；公网 HTTPS、真实设备联网回归和 COS/Agent 尚未处理。

## 2026-09-17：公网 IP 临时联调配置

- 将 `server/compose.yaml` 的监听地址改为环境变量，默认仍为 `127.0.0.1`；服务器临时设置 `FOOTMARKS_BIND_ADDRESS=0.0.0.0`，映射公网 `8000` 端口。
- 服务器本机健康检查通过，容器端口映射显示 `0.0.0.0:8000->8000/tcp`；云安全组放行后，外部健康接口返回正常，真实手机业务回归待验证。
- 使用 `-PfootmarksApiBaseUrl=http://43.135.114.5:8000/` 构建 Debug APK，构建成功；Release 和长期使用仍等待域名 HTTPS。
