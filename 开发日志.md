# 足迹记录 App 开发日志

## 2024年开发记录

### 第一次开发迭代（2024-XX-XX）

#### 开发进度总览
- ✅ 阶段 0：项目初始化
- ✅ 阶段 1：数据层搭建
- ✅ 阶段 2：底部导航框架
- ✅ 阶段 3：添加记录页面
- ✅ 阶段 4：时间线视图

---

## 详细开发记录

### 阶段 0：项目初始化 ✅

**开发时间**: 第一次迭代开始

**实现内容**:
1. 创建 Android 项目，配置 Gradle
2. 添加核心依赖：
   - Jetpack Compose (UI 框架)
   - Room 2.6.1 (数据库)
   - Hilt 2.48 (依赖注入)
   - Coil 2.5.0 (图片加载)
   - Navigation Compose 2.7.6 (导航)
3. 配置 Kotlin 编译器和 Java 17
4. 启用 Core Library Desugaring（支持 API 24 使用 LocalDate）
5. 配置国内 Gradle 镜像（腾讯云）
6. 创建项目包结构

**技术亮点**:
- 使用 `coreLibraryDesugaring` 在低版本 Android（API 24）支持 Java 8+ 时间 API
- 配置阿里云/腾讯云镜像加速依赖下载

**产出**:
- 项目可成功编译
- APK 文件生成位置: `app/build/outputs/apk/debug/app-debug.apk`

---

### 阶段 1：数据层搭建 ✅

**开发时间**: 第一次迭代

**实现内容**:

#### 1. 数据库实体设计
- **CityEntity**: 城市表
  - 字段：id, name, provinceCode, cityCode
  - 用于存储城市信息

- **RecordEntity**: 记录表
  - 字段：id, cityId, type, name, date, rating, cost, notes, photoUris, createdAt
  - 外键关联 CityEntity，级联删除
  - 支持两种类型：景点（ATTRACTION）和美食（FOOD）

#### 2. DAO 接口
- **CityDao**:
  - 基础 CRUD 操作
  - `getCitiesWithRecordCount()`: 查询有记录的城市及记录数量
  - `searchCities()`: 城市搜索（按名称）

- **RecordDao**:
  - 基础 CRUD 操作
  - `getRecordsByCity()`: 按城市查询记录
  - `getAllRecords()`: 查询所有记录
  - 统计功能：总记录数、总城市数、总花费

#### 3. Repository 层
- **CityRepository**: 城市数据访问
- **RecordRepository**: 记录数据访问，包含 `createRecord()` 方法处理照片

#### 4. 工具类
- **PhotoManager**: 照片压缩和存储管理
  - 自动压缩图片到 1080px 长边
  - JPEG 质量 80%
  - 存储到应用内部存储 `/data/data/.../files/photos/`

#### 5. 依赖注入
- **DatabaseModule**: 提供 Room Database 和 DAO 实例
- **FootmarksApplication**: Hilt 应用入口

**技术亮点**:
- Room 数据库外键级联删除
- Flow 响应式数据流
- 类型转换器（RecordType 枚举）
- Hilt 依赖注入

**产出**:
- 完整的数据访问层
- 编译通过，无错误

---

### 阶段 2：底部导航框架 ✅

**开发时间**: 第一次迭代

**实现内容**:
1. **MainActivity**:
   - 使用 Jetpack Compose
   - Scaffold 布局（底部导航栏）

2. **导航结构**:
   - Screen 路由定义（Timeline, Footprint, AddRecord）
   - Navigation Compose 集成
   - 底部导航栏 3 个 Tab

3. **页面占位**:
   - TimelineScreen（时间线）
   - FootprintScreen（足迹）
   - AddRecordScreen（添加记录）

**UI 效果**:
- 底部导航栏显示 3 个图标：时间线、足迹、添加
- 点击切换页面（保持导航状态）
- Material Design 3 设计风格

**产出**:
- 应用主框架搭建完成
- 编译通过

---

### 阶段 3：添加记录页面 ✅

**开发时间**: 第一次迭代

**实现内容**:

#### 1. AddRecordViewModel
- UI 状态管理（AddRecordUiState）
- 表单数据更新方法
- 保存记录逻辑（数据验证 + 持久化）

#### 2. UI 表单组件
- **记录类型选择**: FilterChip 切换景点/美食
- **城市选择器**: OutlinedCard（临时占位，显示弹窗）
- **名称输入**: OutlinedTextField
- **日期选择器**: Material 3 DatePicker
- **评分选择**: 1-5 星 FilterChip，可清除
- **花费输入**: 数字输入框（可选）
- **备注输入**: 多行文本框
- **照片上传**:
  - 横向滚动照片列表
  - 调用系统相册（PickVisualMedia）
  - 照片预览和删除

#### 3. 数据验证
- 必填项检查：城市、名称
- 错误提示显示

#### 4. 保存流程
- ViewModel 调用 Repository
- LocalDate 转换为时间戳
- 照片 URI 保存（逗号分隔字符串）
- 保存成功后返回

**UI 效果**:
- 顶部 AppBar：取消和保存按钮
- 表单项清晰分组
- 类型标签：景点（蓝色）、美食（红色）
- 日期选择器弹窗
- 评分可视化选择
- 照片网格预览，带删除按钮
- 添加照片的 "+" 按钮
- 错误提示卡片

**技术亮点**:
- `@OptIn(ExperimentalMaterial3Api::class)` 使用最新 Material 3 组件
- Activity Result API 调用系统相册
- Coil 图片加载库
- StateFlow 状态管理

**产出**:
- 完整的添加记录功能
- 编译通过

---

### 阶段 4：时间线视图 ✅

**开发时间**: 第一次迭代

**实现内容**:

#### 1. TimelineViewModel
- 从数据库加载所有记录
- 按日期倒序排列
- 加载状态管理

#### 2. UI 列表
- **LazyColumn** 高性能列表
- **RecordCard** 记录卡片：
  - 左侧：照片预览（80x80）或类型图标
  - 右侧：
    - 类型标签（景点/美食）+ 名称
    - 城市名称（临时占位）
    - 日期 + 评分（星星图标）
- 空状态提示："还没有记录，快去添加吧"
- 加载中指示器

#### 3. 交互
- 点击卡片跳转详情（路由已配置，页面待实现）

**UI 效果**:
- 卡片式设计，圆角阴影
- 类型标签颜色区分：
  - 景点：深蓝色 `#3C5A78`
  - 美食：红色 `#FF6B6B`
- 评分金色星星 `#FFB400`
- 照片占位图标：景点用 Place，美食用 Star
- 列表间距 12dp

**技术亮点**:
- Flow 响应式数据加载
- Coil 异步图片加载
- 照片 URI 字符串解析（逗号分隔）
- LocalDate 时间戳转换

**产出**:
- 时间线视图完成
- 编译通过

---

## 当前项目状态

### 已实现功能 ✅
1. ✅ 数据库设计（城市、记录）
2. ✅ 数据访问层（DAO、Repository）
3. ✅ 照片管理（压缩、存储）
4. ✅ 底部导航框架
5. ✅ 添加记录页面（完整表单）
6. ✅ 时间线列表展示

### 待实现功能 ⏳
1. ⏳ 城市选择器（可搜索的城市列表）
2. ⏳ 城市数据源加载（中国行政区划）
3. ⏳ 足迹页面（按城市统计）
4. ⏳ 记录详情页
5. ⏳ 记录编辑/删除功能
6. ⏳ 城市详情页（查看某城市的所有记录）
7. ⏳ 照片压缩实现（PhotoManager 集成）

### 已知问题 ⚠️
1. 城市选择器是临时占位（固定选择"北京"）
2. 时间线中城市名称显示为占位文本
3. 照片上传后未实际压缩存储（仅保存 URI）
4. 缺少记录详情页

---

## 编译状态

**最后编译**: 成功 ✅
**编译命令**: `./gradlew build`
**APK 输出**: `app/build/outputs/apk/debug/app-debug.apk`
**APK 大小**: 约 8-10 MB

---

## 技术架构总结

### 架构模式
- **MVVM**: ViewModel + UI State
- **Repository 模式**: 数据访问抽象层
- **单向数据流**: StateFlow + collectAsState

### 核心技术栈
- **UI**: Jetpack Compose (声明式 UI)
- **导航**: Navigation Compose
- **数据库**: Room 2.6.1
- **依赖注入**: Hilt 2.48
- **异步**: Kotlin Coroutines + Flow
- **图片**: Coil 2.5.0
- **日期**: Java 8 Time API (desugared)

### 项目结构
```
app/src/main/java/com/miracle/footmarks/
├── data/
│   ├── local/
│   │   ├── entity/       # 数据库实体
│   │   ├── dao/          # DAO 接口
│   │   └── util/         # PhotoManager
│   └── repository/       # Repository 层
├── di/                   # Hilt 模块
├── ui/
│   ├── navigation/       # 导航配置
│   ├── screen/
│   │   ├── addrecord/    # 添加记录页面
│   │   ├── timeline/     # 时间线页面
│   │   └── footprint/    # 足迹页面
│   └── theme/            # 主题配置
├── MainActivity.kt
└── FootmarksApplication.kt
```

---

## 下一步开发计划

### 优先级 1（核心功能）
1. 实现城市选择器（带搜索）
2. 加载城市数据源
3. 实现足迹页面（城市列表 + 统计）

### 优先级 2（完善功能）
4. 记录详情页
5. 记录编辑功能
6. 记录删除功能（带确认弹窗）

### 优先级 3（优化）
7. 照片实际压缩和存储
8. 城市名称关联查询
9. 空状态优化
10. 加载动画优化
