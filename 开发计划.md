# 足迹 App 开发计划

更新日期：2026-09-15

## 执行原则

每个阶段完成后都执行对应回归测试，并同步更新 [README.md](./README.md) 与 [开发日志.md](./开发日志.md)。只有实现、构建和设备验证均完成的项目才能标记为“已验收”。

状态说明：

- ✅ 已完成并通过现有验证
- 🧪 已实现，等待完整设备回归或自动化测试
- ⏳ 尚未开始或未完成

## 阶段 0：项目与开发环境 ✅

实现内容：

- Kotlin + Jetpack Compose Android 项目
- JDK 17、Android SDK 34、Gradle 8.4
- Room、Hilt、Navigation Compose、Coil
- API 24 最低版本和 Java Time desugaring
- Gradle Wrapper 与腾讯云 Gradle 分发镜像

验证：运行 `.\gradlew.bat build`，Debug 和 Release 构建均应通过。

## 阶段 1：本地数据层 ✅（数据库迁移除外）

已实现：

- `CityEntity`、`TripEntity`、`RecordEntity`
- `CityDao`、`TripDao`、`RecordDao`
- `CityRepository`、`TripRepository`、`RecordRepository`
- Room 数据库和 Hilt 注入
- `City → Trip → Record` 三层外键关系及数据库级级联删除
- Trip 开始/结束日期校验，以及子记录日期范围校验
- 创建和编辑记录时通过 Room 事务同步 Trip 与 Record
- DAO/Repository 仪器测试覆盖 CRUD、按城市查询、日期约束、事务更新和级联删除

本阶段当时明确排除：

- Room 1 → 2 数据库迁移脚本；该项已在阶段 7 补齐，当前覆盖升级会保留旧记录。

回归：API 34 模拟器执行 `connectedDebugAndroidTest`，8/8 通过；完整 `build`、Debug/Release 构建和 lint 通过。

## 阶段 2：主导航与页面骨架 ✅

已实现：

- 底部三个 Tab：记录、智能规划、个人中心
- 添加记录通过记录页右下角 `+` 进入
- 记录详情和编辑页面路由

智能规划和个人中心的业务内容分别在阶段 8、阶段 9 实现。

回归：切换三个 Tab，进入添加、详情和编辑页面，再通过返回键回到正确页面。

## 阶段 3：基础城市选择 ✅

该阶段曾实现：

- 首次启动预置 20 个常用城市
- 按中文名称包含匹配搜索
- 手动创建未收录城市
- 修复记录保存时因城市不存在导致的外键错误

上述 20 城市预置和手动创建流程已在阶段 6 被完整离线行政区划选择器替代；已有旧城市会在再次选择同一行政区划代码时规范化，不影响关联记录。

回归：选择预置城市、搜索城市、创建新城市，并分别保存记录。

## 阶段 4：记录 CRUD ✅

已实现：

- 创建：城市、类型、名称、日期、评分、花费、备注和照片 URI
- 查询：按日期倒序列表、记录详情和详情中的真实城市名
- 更新：编辑并保存所有当前字段
- 删除：确认对话框和记录删除
- 空列表、加载状态和错误提示
- 记录列表通过联表查询显示真实城市名
- 名称 100 字、备注 2000 字、花费 0～10000000 元、照片最多 9 张的边界校验
- 校验器 JVM 测试和 Room 查询仪器测试

回归：`testDebugUnitTest connectedDebugAndroidTest` 通过，其中 JVM 测试 2 个、模拟器仪器测试 9 个。

## 阶段 5：照片本地化 ✅

已实现：

- 在创建和编辑记录时调用 `PhotoManager`
- 长边压缩至 1080px，JPEG 质量 80
- 保存到 App 内部存储
- 每条记录最多 9 张
- 编辑时清理移除的照片，删除记录时清理全部照片
- 验证重启后照片仍可显示

回归：`testDebugUnitTest connectedDebugAndroidTest` 通过，其中 API 34 模拟器执行 12 个仪器测试；已覆盖 2160px 原图压缩至 1080px、内部目录落盘、编辑替换清理旧照片、删除记录清理全部照片和外部文件删除保护。照片上限由阶段 4 的统一校验器覆盖。重启显示由内部持久路径保证，保留在手工设备回归清单中。

## 阶段 6：完整城市数据 ✅

已实现：

- 确认并留存行政区划数据来源和版本
- 转换为 App assets 中的 JSON
- 接入 3000+ 行政区划记录
- 提供省市级联和搜索
- 首次加载耗时小于 500ms，或采用无需全量入库的读取方案

回归：`testDebugUnitTest connectedDebugAndroidTest` 通过，API 34 模拟器执行 16 个仪器测试。行政区划测试验证 31 个省级和 3056 个县级条目、首次解析低于 500ms、直辖市/自治州/县级市样本、中文搜索路径、同名区县独立保存和旧城市代码规范化；模拟器手工验证选择器打开、省份列表显示和“北京市”回填。

## 阶段 7：Trip 界面与统计 ✅

已实现：

- 记录页按 Trip 开始日期展示旅行卡片和内部景点/美食
- 主 `+` 创建 Trip 并录入城市、开始日期、结束日期和第一条子记录
- Trip 卡片内 `+` 向该次旅行继续添加子记录
- 统计城市数和出行次数
- Room 1 → 2 迁移将每条旧记录完整保留为一条单日 Trip
- 编辑页展示所属旅行日期范围；多日旅行的子记录日期不得越界
- 删除最后一条子记录时自动清理空 Trip

回归：4 个 JVM 测试和 API 34 `Pixel_6` 模拟器 22 个仪器测试通过；覆盖 Trip 分组与排序、城市/出行/花费统计、日期边界、空 Trip 清理及 Room 迁移。设备手工完成新建旅行、追加第二条记录、详情、编辑页日期范围展示、逐条删除及空状态恢复。

## 阶段 8：智能规划对话框架 ✅

已实现：

- 欢迎消息与消息展示区
- 支持 1～4 行草稿输入，草稿通过可保存状态保留
- 点击发送显示“暂未开放，待完善”，并保留原草稿
- 无网络权限、接口或远程 Agent 调用

回归：Compose 仪器测试覆盖普通文本、发送提示、发送后草稿保留和状态恢复；阶段全量回归为 4 个 JVM 测试、24 个 API 34 模拟器测试及完整 `build`。模拟器手工确认欢迎消息、输入区、发送按钮和提示显示正常。

## 阶段 9：个人中心 ✅

已实现：展示本地用户说明、版本信息、城市数、出行次数和人民币总花费；统计直接订阅 Room Flow，记录增删改后自动刷新。

回归：Compose 仪器测试覆盖非空统计和版本展示；模拟器手工验证空数据；Trip DAO 测试覆盖去重城市数、出行次数和可空花费求和。阶段全量回归共 4 个 JVM 测试、25 个 API 34 模拟器测试及完整 `build`。

## 阶段 10：发布前质量 ⏳

- 为 DAO、Repository 和 ViewModel 补充必要测试
- 执行 API 24 与 API 34 设备回归
- 检查崩溃、ANR、照片权限和大量记录滚动
- 更新所有文档中的实现状态和已知限制

验收命令：

```powershell
.\gradlew.bat clean test lint assembleDebug
```

设备回归完成后，在 [开发日志.md](./开发日志.md) 中记录设备型号、Android 版本、日期和结果。
